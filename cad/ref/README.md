# Reference geometry (not printed)

`STS3215_03a.step` — Feetech STS3215 servo with its disc horn and rear idler
wheel, from TheRobotStudio/SO-ARM100 (`STEP/SO100/STS3215_03a.step`,
Apache-2.0). Fetched 2026-09-22. This is the solid `servo_st3215.py` is
derived from and the one the check suite subtracts; every number taken from
it is marked `from STEP` in `params.yaml` and stays VERIFY until a real
servo has been measured with calipers.
