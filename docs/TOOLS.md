# Rocky's tools

<!-- GENERATED by `python -m harness.capabilities --md --out docs/TOOLS.md` from the registry in harness/capabilities.py (D056). Do not edit by hand: change the registry and regenerate; CI fails when this file is stale. -->

Every tool a brain can call, from one registry (`harness/capabilities.py` `REGISTRY`). The local brains' OpenAI tool list (`harness/local_brain.py`, the cockpit's `sim/cockpit_brains.py`, Claude mode) and the MCP server (`harness/server.py`) are generated from it; the cockpit's executor (`/api/tool/<name>`) is checked against it (`sim/cockpit_brains.registry_problems`, run by the tests): a new tool still needs its handler written there.

This page is the no-cockpit snapshot: the canon gesture and chord-word lists (`harness/backend.py`) and every capability on. A running cockpit's lists also carry its saved keyframe gestures and custom chord words.

- snapshot version: `99615678f55b` (sha256 of the snapshot JSON, first 12 hex; it changes when a tool, a list or the envelope changes)
- capabilities: cockpit, eye, memory

## Summary

| tool | kind | gated | requires | offered to |
|---|---|---|---|---|
| `say` | voice | no | — | local brains, MCP |
| `gesture` | motion | yes | — | local brains, MCP |
| `move` | motion | yes | — | local brains, MCP |
| `goto` | motion | yes | — | local brains, MCP |
| `stop` | motion | no | — | local brains, MCP |
| `scan_summary` | query | no | — | local brains, MCP |
| `status` | query | no | — | local brains, MCP |
| `list_gestures` | meta | no | — | local brains, MCP |
| `look` | query | no | eye | local brains, MCP |
| `compose_gesture` | gesture_authoring | yes | cockpit | local brains |
| `check_gesture` | gesture_authoring | no | cockpit | local brains |
| `save_gesture` | gesture_authoring | no | cockpit | local brains |
| `find_object` | motion | yes | eye | local brains, MCP |
| `remember` | memory | no | memory | local brains, MCP |
| `where_is` | memory | no | memory | local brains, MCP |
| `recall` | memory | no | memory | local brains, MCP |
| `go_back_to` | motion | yes | memory | local brains, MCP |
| `forget` | memory | no | memory | local brains, MCP |
| `turn` | motion | yes | cockpit | cockpit executor only |

- **gated**: a spoken line runs it only with the wake word ('pebble, ...') or the operator's confirmation. `stop` is never gated.
- **requires**: `eye` = a camera and a vision model, `memory` = the scene memory, `cockpit` = a running cockpit (`./rocky.sh cockpit`). A backend without it does not offer the tool (absent tool > lying tool).
- **offered to**: local brains = the OpenAI tool list (and Claude mode); MCP = the `rocky` MCP server; cockpit executor only = callable at `/api/tool/<name>`, offered to no model.

## Envelope

| quantity | value | meaning |
|---|---|---|
| `goto_reach_m` | 1.5 | m, the reach the goto text advises ('keep targets within ~N m'); advice, not a refusal (goto refuses only past cockpit_brains.GOTO_MAX_M) |
| `move_max_m` | 1.5 | m, the longest relative move: move refuses beyond it (local_brain.validate_move) |
| `goto_timeout_s` | 40.0 | s, a goto still walking after this ends as stopped=timeout |
| `goto_speed_m_s` | 0.045 | m/s, the speed goto walks at (what it asks, fitted into the walking envelope) |
| `speed_m_s` | 0.0455 | m/s, the top walking speed at any heading (WaveGait.budget) |
| `turn_rad_s` | 0.246 | rad/s, the top turn rate on the spot |
| `step_height_mm` | 24.0 | mm, how high a swinging foot LIFTS (params.yaml gait.step_height, WaveGait.hstep); not the stride length |

Source: gait/pebble_gait.WaveGait().max_command() on cad/params.yaml.

## Robot

| field | value |
|---|---|
| `legs` | 5 |
| `joints_per_leg` | 3 |
| `joint_names` | yaw, hip, knee |
| `tool_joints` | claw |
| `stations_deg` | 90.0, 162.0, 234.0, 306.0, 378.0 |
| `actuators` | 20 |
| `topology_hash` | 7f066d9bd8c0 |
| `params_rev` | 0.1/D052 |

Source: gait/rocky_model.robot() (cad/params.yaml robot:).

## Gestures (as of generation)

`jazz_hands`, `fist_bump`, `beckon`, `wave`, `bow`, `look_around`, `shake`, `sit`, `turn_in_place`, `sidestep`

Take `direction` left|right: `turn_in_place`, `sidestep`.

## Chord words (as of generation)

`acknowledge`, `alarm_help`, `amaze`, `confused`, `curious_question`, `determined`, `discovery`, `error`, `found_it`, `greeting`, `low_battery`, `no`, `sleepy`, `startup`, `thinking`, `yes`

## Tools

### `say`

- kind: voice; gated: no; requires: —; offered to: local brains, MCP; backend method: `say`
- annotations: readOnlyHint=false, idempotentHint=true
- MCP arguments: `word` string

| parameter | type | required | description |
|---|---|---|---|
| `word` | string, one of the chord words (the live list) | yes | a chord-speak word |

Description (local brains):

> Speak one chord-speak word through the speakers (the robot's only voice — it never speaks human words).

Description (MCP):

> Speak one chord-speak word through Rocky's voice. word: one of acknowledge, alarm_help, amaze, confused, curious_question, determined, discovery, error, found_it, greeting, low_battery, no, sleepy, startup, thinking, yes. Unknown words are refused with the lexicon in the error (Rocky never speaks human words — canon).

### `gesture`

- kind: motion; gated: yes; requires: —; offered to: local brains, MCP; backend method: `gesture`
- annotations: readOnlyHint=false
- MCP arguments: `name` string, `direction` string = ""
- note: MCP direction is a plain string defaulting to '' (no enum); the MCP text lists the gestures, the local-brain text puts them in the enum instead.

| parameter | type | required | description |
|---|---|---|---|
| `name` | string, one of the gestures (the live list) | yes | gesture name |
| `direction` | string, one of: left, right | no | — |

Description (local brains):

> Perform one gesture and wait for it to finish. Refused with error='busy' while walking: stop first. direction left|right applies to turn_in_place and sidestep only (turn_in_place turns on the spot for ~5 s; left = counter-clockwise).

Description (MCP):

> Perform a gesture. Available: jazz_hands, fist_bump, beckon, wave, bow, look_around, shake, sit, turn_in_place, sidestep. direction 'left'|'right' applies to turn_in_place and sidestep only (turn_in_place turns on the spot for ~5 s; left = counter-clockwise). Refused with error='busy' while walking — stop() first. list_gestures has the current list.

### `move`

- kind: motion; gated: yes; requires: —; offered to: local brains, MCP; backend method: `move`
- annotations: readOnlyHint=false
- MCP arguments: `forward_m` number, `left_m` number = 0.0
- note: The harness backends have no move method: the MCP server and local_brain.call_tool run harness.local_brain.move_via (status + goto; a cockpit's own /api/tool/move). The cockpit executor runs Brains.move.

| parameter | type | required | description |
|---|---|---|---|
| `forward_m` | number | yes | meters, + ahead / - back (0.3 = 30 cm) |
| `left_m` | number | no | meters, + left / - right (default 0) |

Description (local brains):

> Relative move in the robot's OWN frame, in METERS: forward_m + ahead / - back, left_m + left / - right (default 0). 'forward 30 cm' = move(forward_m=0.3); 'back up 20 cm' = move(forward_m=-0.2); 'half a meter to your left' = move(forward_m=0, left_m=0.5). It reads the robot's pose and heading itself (no status call, no trigonometry) and walks there as a goto: every guard applies and it ends the same way, stopped= arrived | cliff | stuck | blocked | timeout | user | preempted | FELL (a veto is a NORMAL result: report it, do not retry). At most 1.5 m. goto is for MAP coordinates.

Description (MCP):

> Relative move in the robot's OWN frame, in METERS: forward_m + ahead / - back, left_m + left / - right (default 0). 'forward 30 cm' = move(forward_m=0.3); 'back up 20 cm' = move(forward_m=-0.2); 'half a meter to your left' = move(forward_m=0, left_m=0.5). It reads the robot's pose and heading itself (no status call, no trigonometry) and walks there as a goto: every guard applies and it ends the same way, stopped= arrived | cliff | stuck | blocked | timeout | user | preempted | FELL (a veto is a NORMAL result: report it, do not retry). At most 1.5 m. goto is for MAP coordinates. One motion intent at a time; a newer move or goto preempts it.

### `goto`

- kind: motion; gated: yes; requires: —; offered to: local brains, MCP; backend method: `goto`
- annotations: readOnlyHint=false
- MCP arguments: `x` number, `y` number

| parameter | type | required | description |
|---|---|---|---|
| `x` | number | yes | — |
| `y` | number | yes | — |

Description (local brains):

> Walk to (x, y) in METERS, map frame (the robot starts at (0, 0) facing +x; +y is its left) — a point the operator gives as coordinates or a remembered position; for a move relative to the robot ('forward 30 cm') use move instead. Blocks until it ends and returns stopped= arrived | cliff (the void reflex vetoed it: do NOT retry toward it) | blocked (EITHER the lidar saw an obstacle in the way and 2 detours — a 45 deg veer, then a sidestep — did not get past it: the result's obstacle has range_m and bearing_deg, body frame, 0 = ahead, + = left; pick a target that avoids that side — OR a guard refused to start: read detail) | stuck (no progress for 3 s: something the lidar cannot see, lower than the puck, is in the way — look, then pick a different target; never re-send the same one) | timeout (too far: ~0.045 m/s, 40 s cap, keep targets within ~1.5 m) | user (stop was called) | preempted (a newer goto took over) | FELL. A veto is a NORMAL result: report it.

Description (MCP):

> Walk to (x, y) in METERS, map frame (the robot starts at (0, 0) facing +x; +y is its left) — a point the operator gives as coordinates or a remembered position; for a move relative to the robot ('forward 30 cm') use move instead. Blocks until it ends and returns stopped= arrived | cliff (the void reflex vetoed it: do NOT retry toward it) | blocked (EITHER the lidar saw an obstacle in the way and 2 detours — a 45 deg veer, then a sidestep — did not get past it: the result's obstacle has range_m and bearing_deg, body frame, 0 = ahead, + = left; pick a target that avoids that side — OR a guard refused to start: read detail) | stuck (no progress for 3 s: something the lidar cannot see, lower than the puck, is in the way — look, then pick a different target; never re-send the same one) | timeout (too far: ~0.045 m/s, 40 s cap, keep targets within ~1.5 m) | user (stop was called) | preempted (a newer goto took over) | FELL. A veto is a NORMAL result: report it. One motion intent at a time; calling goto again preempts.

### `stop`

- kind: motion; gated: no; requires: —; offered to: local brains, MCP; backend method: `stop`
- annotations: readOnlyHint=false, idempotentHint=true
- MCP arguments: none
- note: Never gated: a stop by voice needs no wake word.

No parameters.

Description (local brains):

> Safe-stop immediately (PLANT->BRACE). Always accepted.

Description (MCP):

> Safe-stop NOW (always accepted): finish the current step, brace into a full-contact crouch, hold. Resolves any in-flight goto with stopped='user'.

### `scan_summary`

- kind: query; gated: no; requires: —; offered to: local brains, MCP; backend method: `scan_summary`
- annotations: readOnlyHint=true
- MCP arguments: none

No parameters.

Description (local brains):

> What the lidar sees: nearest obstacles by direction (walls and objects yes; holes in the floor no).

Description (MCP):

> Summarize the latest lidar scan: point count, open frontiers (bearing/range), nearest obstacle.

### `status`

- kind: query; gated: no; requires: —; offered to: local brains, MCP; backend method: `status`
- annotations: readOnlyHint=true
- MCP arguments: none

No parameters.

Description (local brains):

> Robot status: pose (x, y m; yaw deg), mode, reflex state.

Description (MCP):

> Robot status: pose, mode (idle|walking|safe_stop), battery voltage (mocked until hardware), recent events.

### `list_gestures`

- kind: meta; gated: no; requires: —; offered to: local brains, MCP; backend method: `list_gestures`
- annotations: readOnlyHint=true
- MCP arguments: none
- note: The MCP server answers from harness.backend GESTURES/SIGNED when the backend has no list_gestures.

No parameters.

Description (local brains):

> The gestures this robot knows right now (saved ones included).

Description (MCP):

> The gestures the robot knows RIGHT NOW (saved keyframe gestures included) and which ones take a direction.

### `look`

- kind: query; gated: no; requires: eye; offered to: local brains, MCP; backend method: `look`
- annotations: readOnlyHint=true
- MCP arguments: none
- note: Local brains: offered when build_tools(look=True) (the cockpit always); MCP: when the backend has look. The cockpit executor also takes an undeclared 'model' argument.

No parameters.

Description (local brains):

> Look through the robot's eye camera: a vision model describes what is in front of the robot (obstacles, open floor, objects).

Description (MCP):

> Look through the robot's eye camera: a vision model describes what is in front of the robot (obstacles, objects, open floor).

### `compose_gesture`

- kind: gesture_authoring; gated: yes; requires: cockpit; offered to: local brains; backend method: `compose_gesture`
- annotations (not published: no MCP tool today): readOnlyHint=false
- note: Cockpit brains only; not an MCP tool today.

| parameter | type | required | description |
|---|---|---|---|
| `name` | string | yes | short snake_case name |
| `description` | string | no | what the gesture should look like |
| `keyframes` | array of object {t*, body, yaw, dz, arm, claw, say, ease} | yes | — |
| `loop` | boolean | no | — |

Description:

> Create a NEW gesture from keyframes (the owner describes it; you write the frames). It is checked against the robot's real limits (joint range, servo speed, balance, self-contact); when feasible it is previewed once on the robot and kept as a draft until save_gesture. Frame fields (all optional except t; missing = standing): t seconds from start (first frame {"t": 0} = standing); body [dx, dy, dz] mm body shift, feet planted (z < 0 crouches; +-30 mm is a lot); yaw deg body twist (+-15); dz [5] mm extra crouch per leg; arm {"leg": [yaw, hip, knee] deg} lifts that leg as an arm (yaw -40..40, hip -70..90 (up), knee -150..-20); claw [5] 0..1 open; say a chord word cue; ease smooth|linear|hold. Legs: 0 = left (+y), then counter-clockwise: 1 rear-left, 2 rear-right, 3 front-right, 4 front-left. Keep >= 4 feet down (raise ONE leg; shift the body away from it first, body [0, -15, 0] for leg 0), give each move >= 0.6 s. Example wave with leg 0: [{"t":0},{"t":1.0,"body":[0,-15,0]},{"t":1.8,"body":[0,-15,0],"arm":{"0":[0,70,-60]}},{"t":2.6,"body":[0,-15,0],"arm":{"0":[25,70,-60]}},{"t":3.4,"body":[0,-15,0],"arm":{"0":[-25,70,-60]}},{"t":4.2,"body":[0,-15,0],"arm":{"0":[0,70,-60]}},{"t":5.0,"body":[0,-15,0]},{"t":5.8}]

### `check_gesture`

- kind: gesture_authoring; gated: no; requires: cockpit; offered to: local brains; backend method: `check_gesture`
- annotations (not published: no MCP tool today): readOnlyHint=true
- note: Cockpit brains only; not an MCP tool today.

| parameter | type | required | description |
|---|---|---|---|
| `spec` | object | yes | — |

Description:

> Check a keyframe gesture spec {name, keyframes, loop?, end?} against the robot's limits WITHOUT moving; returns the report lines.

### `save_gesture`

- kind: gesture_authoring; gated: no; requires: cockpit; offered to: local brains; backend method: `save_gesture`
- annotations (not published: no MCP tool today): readOnlyHint=false
- note: Cockpit brains only; not an MCP tool today. Changes the live gesture list.

| parameter | type | required | description |
|---|---|---|---|
| `name` | string | no | optional new name |
| `overwrite` | boolean | no | — |

Description:

> Save the last composed (previewed) gesture so it can be played by name. Only when the operator asked to keep it.

### `find_object`

- kind: motion; gated: yes; requires: eye; offered to: local brains, MCP; backend method: `find_object`
- annotations: readOnlyHint=false
- MCP arguments: `name` string, `max_steps` integer = 6
- note: Local brains: part of cockpit_brains.EXTRA_TOOLS (offered with the extras, whatever look says); MCP: when the backend has find_object.

| parameter | type | required | description |
|---|---|---|---|
| `name` | string | yes | what to find, in plain words: 'ball', 'box' |
| `max_steps` | integer | no | looks before giving up (default 6, max 16) |

Description (local brains):

> Find an object by name with the eye and walk up to it (e.g. 'find the ball', 'go to the box'). It loops by itself: look (a vision model boxes the object; its bearing and distance come from the camera geometry), then walk 0.1-0.4 m toward it (an ordinary goto: every guard applies), or turn 30 deg to scan when it is not seen or the sighting is unsure; it ends found (within ~0.25 m) | not found | stopped (a goto came back cliff / blocked / stuck: report it, do not retry blindly). Takes up to ~1-2 minutes.

Description (MCP):

> Find an object by name with the eye and walk up to it ('ball', 'box'). Runs in the cockpit by itself: look (a vision model boxes it; bearing and distance come from the camera geometry), then a 0.1-0.4 m goto toward it (every guard applies) or a 30 deg scan turn when it is unseen or unsure. Ends found (within ~0.25 m) | not found | stopped (a goto came back cliff / blocked / stuck: a veto, report it). Up to ~1-2 minutes; stop ends it.

### `remember`

- kind: memory; gated: no; requires: memory; offered to: local brains, MCP; backend method: `remember`
- annotations: readOnlyHint=false
- MCP arguments: `note` string

| parameter | type | required | description |
|---|---|---|---|
| `note` | string | yes | the fact, in the operator's words |

Description (local brains and MCP):

> Remember a fact the operator states, in their words ('the charger is by the door'). 'the X is here' / 'this spot is X' / 'call this spot X' pins X at the robot's current position; 'the X is at (1.0, 0.2)' pins map meters (the floor is +-6 m); anything else is kept as a note that recall finds by its words. Only for what the operator tells you — sightings are remembered automatically.

### `where_is`

- kind: memory; gated: no; requires: memory; offered to: local brains, MCP; backend method: `where_is`
- annotations: readOnlyHint=true
- MCP arguments: `name` string

| parameter | type | required | description |
|---|---|---|---|
| `name` | string | yes | the object, in plain words: 'ball' |

Description (local brains and MCP):

> Where is a named object, from memory (no looking, no walking): its map position (meters), how long ago it was seen, confidence and source (find = the eye's box geometry; user = the operator pinned it; look = a vague description). stale=true: seen over 10 min ago or before the last reset, so it may have moved — find_object re-checks. known=false when it was never located — then call find_object.

### `recall`

- kind: memory; gated: no; requires: memory; offered to: local brains, MCP; backend method: `recall`
- annotations: readOnlyHint=true
- MCP arguments: `query` string = "", `k` integer = 5

| parameter | type | required | description |
|---|---|---|---|
| `query` | string | no | — |
| `k` | integer | no | how many entries (default 5, max 20) |

Description (local brains and MCP):

> Search the robot's memory of this world — what it saw, found, was told, and which guards fired — for a query ('ball', 'charger', 'cliff'); returns the most relevant entries with their age. An empty query returns recent entries, the operator's notes and things near the robot first.

### `go_back_to`

- kind: motion; gated: yes; requires: memory; offered to: local brains, MCP; backend method: `go_back_to`
- annotations: readOnlyHint=false
- MCP arguments: `name` string
- note: A memory tool (cockpit_brains.MEMORY_TOOLS: Brains.mem_go_back_to) that walks.

| parameter | type | required | description |
|---|---|---|---|
| `name` | string | yes | the remembered object: 'ball' |

Description (local brains and MCP):

> Walk back to a remembered object or place ('go back to the ball', 'go back to the start' = where the robot spawned): ordinary gotos toward its remembered position (every guard applies), stopping ~0.4 m short of it (a place the operator pinned with 'X is here': onto it). Refused when it is not remembered, only vaguely, or only from before the last reset — then call find_object. A stale sighting (> 10 min) is walked to but flagged. It does NOT re-check with the eye: call look or find_object afterwards if the object may have moved.

### `forget`

- kind: memory; gated: no; requires: memory; offered to: local brains, MCP; backend method: `forget`
- annotations: readOnlyHint=false, destructiveHint=true
- MCP arguments: `name` string
- note: Not gated, but the cockpit refuses a spoken forget of 'all' without the wake word.

| parameter | type | required | description |
|---|---|---|---|
| `name` | string | yes | the object, or 'all' |

Description (local brains and MCP):

> Forget one named object (and every memory that mentions it), or everything with name 'all'. A pronoun ('that', 'it') forgets nothing: name the thing. Only when the operator asks.

### `turn`

- kind: motion; gated: yes; requires: cockpit; offered to: cockpit executor only; backend method: `turn`
- annotations (not published: no MCP tool today): readOnlyHint=false
- note: In the cockpit executor (TOOL_NAMES, /api/tool/turn) and GATED, offered to no model.

| parameter | type | required | description |
|---|---|---|---|
| `deg` | number | yes | degrees, + = left; 5..180 in magnitude |

Description:

> INTERNAL (never offered to a model): turn on the spot by about deg degrees (+ = left, counter-clockwise) with the turn_in_place gait, timed to the measured turn rate; find_object's scan turn, replayed by recordings. Reports the measured turn.
