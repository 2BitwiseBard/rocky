# Design backlog — evaluated 2026-07-30 (session 4)

*Each idea got an hour-or-less verdict: sim test where physics could answer,
CAD where the part was obviously cheap, spec-and-defer where it depends on
hardware we don't have. Statuses: SHIPPED (in `cad/`, print-ready),
NEGATIVE (tested, don't build), DEFERRED (spec'd, waiting on a dependency).*

| # | idea | verdict | evidence / where |
|---|---|---|---|
| B1 | Ramped TPU shin fairing | **NEGATIVE (for now)** | `sim/run_fairing.py` + `fairing_results.json`: helps only at 45 mm rubble (2/4 vs 1/4 crossings), *slightly hurts* at 35 mm (3/4 vs 4/4), and **degrades the stuck-watchdog's 40 mm result (2/4 vs 4/4)** — the fat shin closes the gaps the high-step retry threads through. Watchdog alone owns the 33–44 mm band; 45 mm+ is outside the mission envelope. Revisit only if real rubble chews the bare shin. |
| B2 | Belly skid rails | **SHIPPED** | `part_smallwins.belly_skid` — sacrificial, deck-grid bolts, rounded scrape face. Print 2 in PLA (they're meant to die). |
| B3 | Trim-mass bosses | **SHIPPED** | `part_smallwins.trim_cup` + lid — M3-washer ballast cups on the deck grid. CoM trimming with measured masses beats taping coins (D018 taught us CoM millimeters matter). |
| B4 | IMU TPU grommets | **SHIPPED** | `part_smallwins.imu_grommet` (×4) — isolates the BNO085 from gait-frequency deck ring; the avionics tray (I4) already has the Ø4.8 holes. |
| B5 | Calibration comb | **SHIPPED (as 2 gauges)** | `part_smallwins.calib_gauge_hip` (tower cradles the knee-servo case bottom at leg z 45.65 ⇒ hip = 0) + `calib_gauge_knee` (V-block plumbs the tube at x = 140 ⇒ knee = −90). Registers on the bench jig; `bench/calibrate_centers.py --knee-at -90` matches. |
| B6 | Palm magnets + shell tool docks | **HALF-SHIPPED (session 5)** | Shell side DONE: vertical I6 male bars at ±27° on every carapace sector (`part_shell.py`) — any dovetail shoe (B11 whisker, future docks) slides on today. Palm side still waits for hand v0.3. |
| B7 | Fingernail lip + TPU tip caps | **DEFERRED to hand v0.3** | Fingertip geometry changes ride the post-caliper rebuild; TPU foot pad (shipped, `part_footpad.py`) covers the walking-wear case meanwhile. |
| B8 | LED ring channel | **SHIPPED (session 5)** | Wall-following groove around carapace tier 2 (`part_shell.py`) — 8.4 mm tall, ~2.4 deep, chamfered top, feed notch into the cavity at the az-33 web; powers from an I5 XT30 spare tap per the harness plan. (A circular groove missed the rocky wall at the webs — the pentagon factor strikes again; it follows the outline function now.) |
| B9 | Shell vent gills | **SHIPPED (session 5)** | Real gills on the real sectors: 3 angled slots per side through the tier-1 wall, over the leg bays, placed against the queried wall radius. |
| B10 | Per-leg numbering | **SHIPPED** | Deck v0.3: k+1 engraved dots at each station + north arrow (font-free, slicer-proof). Legs themselves stay identical (the whole point of radial symmetry) — the DECK carries identity. |
| B11 | Whisker mounts | **SHIPPED** | `part_smallwins.whisker_shoe` — I6 dovetail shoe, 2× Ø1.3 piano-wire bores at ±20°. Phase-3 toy, zero-cost now. |

**Order addendum implications** (fed into the Batch-3 sheet): M3 washers
(B3 ballast + the deck v0.4 shell-magnet strikes), no TPU fairing spool
needed (B1 negative), piano wire 0.8 mm (B11, hardware-store), magnets
Ø6×3 (B6/I3 — already on the connector addendum for the panel standard).

## Session-5b additions

| # | idea | verdict | evidence / where |
|---|---|---|---|
| B12 | Lidar mount on the hatch cap | **SPEC'D, blocked on the pull-vs-LD19 decision** | Cap variant with a puck seat + bolt-circle bosses + wire drop into the cavity. Every dim is VERIFY until a puck is in hand — params.yaml gets a `lidar:` block when it arrives; building it now would be guessing in plastic. |
| B13 | Femur/tube cable clips | **SHIPPED (5c)** | `part_clips.py`: snap tube_clip (80 % gap) + link_clip, both with 4×6 wire tunnels + zip slots. ID fine-tune rides the caliper regen. |
| B14 | Charging dock | **MECHANICAL SHIPPED (5c); electrical Batch-2** | `part_dock.py`: walk-on plate + funnel rails (±8 mm capture → ±0.8, the XT60 float's own tolerance), shin bumper, plug tower with VERIFY-height XT60 carrier; every margin printed (feet land on ground, plug meets the 20 mm-crouch mate plane within 2 mm). Robot-side port options spec'd in the docstring — decide with the BEC/harness. |
| B15 | Fingertip grip serrations (hand v0.3) | **QUEUED with the caliper rebuild** | Transverse V-grooves on the inner cone faces; zero-cost boolean once hand v0.3 regenerates. Deliberately NOT sculpted against nominal geometry — that would be printing guesses. |
| B16 | Camera-behind-gill bracket | **SPEC'D (VISION_PLAN.md)** | Pi Cam 3 wide on an internal bracket, lens through a gill-slot aperture — vision without breaking the eyeless canon face. Prints with the shell regen once a camera is in hand (dims VERIFY). |

## Session-6 additions

| # | idea | verdict | evidence / where |
|---|---|---|---|
| B17 | Printed ST3215 servo blanks | **SHIPPED (s6)** | `part_servo_blank.py` — exact-envelope PLA stand-ins so the leg chain assembles WEEKS before the servo order lands (the servos are the joints). Boolean-proven in all three cradles (coxa base / fork rails / knee carrier, 0.00 mm³ each); horn disc carries the true M2 BCD so couplers and femur_link bolt on. Retire on servo-arrival day. |
| B18 | Beckon / "come-here" gesture | **SHIPPED (s6, same day)** | `pebble_gestures.beckon` + `sim/run_beckon.py` (narrated video): arm up-out, three 80°-knee curls with the claw opening on each pull-in, synced to a rising `curious_question`. Lesson en route: position-waypoint poses near joint limits SATURATE silently — expressive poses are authored in JOINT space now (D035). Name-motif greeting still rides chord-speak v0.3. |
| B19 | Two-sided hip/knee brackets (rear idler) | **SHIPPED (D047, 2026-09-22)** — the STEP has the idler; femur plate B rides it at hip AND knee, the fork rides it at the yaw. Was: | D046 leaves hip and knee single-sided: all leg bending goes through the servo output shaft. VERIFY on servo arrival whether the ST3215 has a rear idler boss/hole; if yes, the femur link grows a second arm riding a bearing on it and the knee carrier likewise. |
| B20 | Case-screw servo retention | **SHIPPED (D047)** — `servo_mount.servo_cup`: four self-tappers into the rim holes measured on the STEP; lips + straps retired. Was: | D046 lips + straps are independent of the (unmeasured) case holes. Measure the ST3215 case screw pattern; replace strap tap bores with case screws through the rail walls. |
| B21 | Printability: cantilever rule | **SHIPPED (2026-09-17)** | `check_printability.py`: an overhang blob whose boundary mostly does not touch the layer below is a CANTILEVER (vs a bridge over a bore); ≥ 3 mm with a 'no support' plan is a hard failure. Regression: the v0.1 servo blank's horn disc (7 mm) fails, v0.2 passes. |

## Session-9 additions (2026-09-22 review, D047)

| # | idea | verdict | evidence / where |
|---|---|---|---|
| B22 | Servo swap without pulling the femur | **DEFERRED** | The knee cup's two horn-face rim screws sit under plate A's beam; a knee/hip servo swap = plate B (4) + coupler clamps (4) + plate A off, then the cup's 4 screws. `check_assembly` models the screws as driven before the femur goes on. Revisit only if swaps turn out frequent. |
| B23 | Horn hole radius + thread | **COUPON (batch 1)** | The STEP reads 14.0–15.6 BCD and RobotShop's kit list says M3 × 6; SO-ARM100 lore says M2. `coupon_yaw_hub` + `horn_coupler` carry radial slots and M3/M2+washer counterbores; one screw on the real horn settles `params.servo_st3215.horn_bcd` / `horn_screw_m`. |
| B24 | URDF masses from the CAD budget | **SHIPPED (2026-09-22)** | `generate_urdf.py` reads `sim/mass_budget.json` (was: session-2 constants → 3.09 vs 2.7 kg parity failure) and expands its own macro when xacro is not installed. |
| B25 | Plain foot before the iris hand | **RECOMMENDED** | The hand-as-foot (D005) is the most complex, least-tested subsystem and takes every ground strike. The I2 bayonet already accepts a plain TPU foot on the SEA stub; walk on that first, fit the hand as a tool later. |
| B26 | Heat-set inserts where screws are opened often | **OPTION** | D047 uses thread-forming M3 in PETG for plate B's four bosses and the tube pinch bolt. If plate B comes off more than twice, the Ø8 bosses take the Ø4.6 × 5.7 insert pocket already in `params.print`. |
| B27 | `deck_t` 4.0 in params vs 6.0 hard-coded in `iface.py` / `part_deck.py` | **OPEN** | One number, two values; the params one is dead. Unify before the deck reprint. |
| B28 | Fit ladder → params | **BLOCKING** | The ladder was printed weeks ago and nothing consumes it: `params.print` still holds the initial guesses. First item of `docs/PRINT_PLAN_2026-09-22.md`. |
| B29 | Shove model: half-sine at the shell rim, quoted in N·s and BW | **SHIPPED** (D048) | `sim/shove.py`; the playground `push`, the fallen demo and `shove_envelope.py` use it. The push-envelope experiment scripts keep the CoM pulse as decision records — re-run them under the new model only if a decision needs it (the BW invariant of D039 still holds). |
| B30 | Righter without the staircase | **NEGATIVE ×2, OPEN** (D048) | `recover1` is a 50 Hz staircase (73 % of tick moves pinned at 5 rad/s, ~10 reversals/s per joint — `sim/audit_righter.py`). Reward v3 (smoothness cost) + 3 rad/s: scratch 2/20, warm-from-recover1 4/20 vs 7/20 — smoother (58 %, 6.5/s, 2.1°/tick) but fewer handoffs at 3–5 M steps. Next candidates, in order: an action low-pass filter INSIDE the env so the policy trains on it; a side→back curriculum (no checkpoint rights from the back; the planted ramp does, which is what the D048 stall rule exploits); v3 at 10 M. The bar: ≥7/20 with the warm run's smoothness. |
| B32 | Bench bring-up through the cockpit | **OPEN** (D051 shipped the tooling, mock-verified only) | First real servo on the bus: `rocky.sh cockpit` → Hardware → connect the adapter port → scan → set ID one servo at a time → center each joint at the jig → dir test with jog → `robot → sim` to confirm the IK frame → `sim → robot` with the stream cap at 200 c/s for the first stance. Then: does the real leg track a gesture at the servo model's numbers (turn the model on, compare)? Record the measured no-load slew and latency into `servo_model.DEFAULTS`. Later: hands (SCS0009) in the bridge, a per-leg contact switch feeding `Playground._probe` so the real foot probes like the sim foot. |
| B31 | Cockpit follow-ups | **MOSTLY SHIPPED** (D050: contact-seeking stance, map, recording/replay, saved + random worlds, walker, voice, phone layout; open: websocket transport, climbing a step taller than the swing height) | Websocket instead of MJPEG+SSE if the LAN case matters (phone over tailscale works as is, just heavier); a top-down map with the lidar scan and the goto target; recording from the cockpit (`record on` already works in the console, the clip lands in sim/); a voice button (whisper-server) in the chat; save/load world specs as JSON files; the `walk` residual policy as a selectable gait (needs a PebbleEnv obs builder in the loop). |

## Session-9f additions (2026-09-24 review, D052)

| # | idea | verdict | evidence / where |
|---|---|---|---|
| B33 | CAD-derived CoM / inertia, an SEA slide joint, ToF floor sensors | **PROPOSED** | Three things the D052 model still guesses. (a) Masses come from CAD volumes, but each link's CoM and inertia are primitive stand-ins (torso = two cylinders split 0.75 / 0.25, shin CoM at 0.46·L3); build123d gives each part's centroid and inertia tensor, so `mass_audit` could write them and `build_mjcf` / `generate_urdf` / the feasibility CoM model (which mirrors `build_mjcf.leg_xml`) read them. Matters most for `manip_adjacent`, whose CoM margin is 17.5 mm. (b) The SEA cartridge (D010: 7 mm travel) is rigid in the sim: a slide joint with the spring rate would make landings, shoves and the foot switch (which closes on spring compression) physical, instead of a force threshold on a rigid sphere. (c) A downward ToF sensor per leg (or on the shell) would see a void *before* a foot probes into it — the lidar plane (~0.18 m) is blind below, and the probe-based void guard, even with the D052 touchdown gate, still falls in a lip band of approach angles (7 of 310 on a 1° grid, D052a): a foot centred 0–9 mm past the edge sits on the corner, closes its switch and slides off — nothing a foot switch can see, which a look-ahead sensor would. (a) is software only; (b) needs the spring rate from the bench; (c) needs a part choice, a mount and pins on the harness. |
| B34 | Overnight righter retrain on the D052 contract | **NEGATIVE ×1, OPEN** (first run 2026-09-24: `recover6_d052`, 3 M steps, 0/20 hardware handoff, supervisor 11/20 = no-righter 11/20; recipe below) | Train on the amended model (fingerprint `ceb63a1254c3`: leg forcerange = stall 2.94 N·m, the continuous budget as `rl_common.ThermalProxy`), not the `5a32…` 1.911 clip. The baseline to beat there is `recover1` re-measured on it: 0/20 `handoff_ok`, 5/20 legacy (3/20 with the clip: noise), system 20/20 vs 11/20. From cold the proxy never trips inside a 6 s episode, so a run that should learn to live with a derated joint needs `thermal_heat0` warm starts — an env argument with no `train_ppo` flag yet; leave it off for the first run. Only two 2 000-step smokes exist under obs v2; `recover1` cannot be warm-started (obs 39 → 57, the trainer refuses). Recipe: `./rocky.sh train-recover recover6 --total-steps 3000000` (= `--env recover --reward v2 --log-std-max -0.5 --num-envs 8`, with the D052 defaults `--servo random --ema-alpha 0.4 --rate-limit 4.0`, DR + obs noise on; add `--device cpu` to keep the GPU free; ~450 steps/s on the 2-env smoke, so size the night by the first log lines). `./rocky.sh jobs` to watch; `ep_len` falling below 300 = episodes ending in success; `ent` must not climb (D045). Then, in order: `./rocky.sh eval-recover recover6` (nominal, `handoff_ok`), `--randomize`, `--supervisor` (look at the `handoff` exits, not only stood), `MUJOCO_GL=egl python sim/audit_righter.py sim/runs/recover6/latest.pt --servo nominal`. Bar: B30. Same night if the GPU is free: a D052 walker, `./rocky.sh train-walk walk1` (commands budgeted, rim shoves 10–35 N), compared with `eval-walk --compare-zero` — every walker on disk is pre-D052 and has no envelope. The obs-noise levels and the Δa weight are guesses; a negative result gets written down like the others. |
| B35 | The onboard loop | **OPEN** (sim side ready; nothing on the Pi) | What stands between the sim's control loop and the real robot: an **IMU driver** (BNO085 → the same gravity / gyro `sim_imu.SimIMU` produces); **foot switches** (the SEA microswitches → `perception/contacts` semantics, 2.0 / 1.0 N hysteresis to be re-read as switch states); a **50 Hz loop on the Pi** running WaveGait + `budget` + ReflexSupervisor (with `q_meas`, `probe_dz`, `handoff_ok`) + the Playground's guards, writing through `SoftStream`; a **host watchdog** (laptop ↔ Pi heartbeat; loss → planted stop, then limp) — the bridge's 2 s sim heartbeat is the laptop-side half; a **HardwareBackend** (`harness/hw_backend.py`: the six MCP tools on `PebbleRobot`, so `rocky.sh chat` / the brains drive the robot); **`cockpit --remote`** (the cockpit as the UI for the robot's loop, not a second controller — the mirror today streams sim targets, which is the bench tool, not the runtime). Also a numpy / ONNX path for the righter (torch on the Pi is not wanted). |
| B36 | The robot as data | **STEP 1 SHIPPED (D053), steps 2-10 OPEN** | `docs/ROBOT_AS_DATA.md` (design) and `docs/DESIGN_CHANGE_GUIDE.md` (how to change the design today). Step 1: params `robot:` + `rocky_model.robot()` / `validate()` / `actuator_order()` / `id_table()` / `topology_hash()`; stations, leg count and actuator order in `build_mjcf` and `generate_urdf`; `pebble.xml` byte-identical, fingerprint `ceb63a1254c3`. Remaining, in order, each its own commit with the suite green: **(2)** generators build the chain (bodies / joints / axes / offsets, kp / kv / armature / protocol into `actuators:`, a generated `rocky.ros2_control.xacro`); **(3)** close the fail-open paths (feasibility codes from joint names, `check_urdf_parity` n_lim, mock ids from `id_table()`, the driver's broad `except` into 3-joint limits, a bridge that refuses an uncovered id, no silent hand-budget fallback); **(4)** actuators by name (`sim/model_index.py`) instead of `ctrl[:15]` / `ctrl[15:20]` in the playground, both RL envs, `rl_common`, `sim_lidar`, `servo_model` — BEFORE any topology change; the cockpit / harness slices are handed to that workflow; **(5)** checkpoint safety: topology hash stamped, refuse on mismatch, `ROCKY_STRICT_FP`, `eval_recover --accept-robot`, fingerprint extended (a D-number); **(6)** `gait/leg_kin.py`: generic FK / IK, analytic fast path by chain shape, numeric IK with a redundancy rule and a measured Pi budget, replacing four FK copies; **(7)** materials / part_material / part_fill in params + one `sim/body_layout.py` (a D-number: fixes the PETG parts weighed as PLA); **(8)** `cad/check_params_parity.py` (tibia stack + tube cut length BEFORE the first leg is assembled, foot literals, deck station count, sweeps from `joints.pos_deg`); **(9)** `rocky.sh regen` + SIM_GUIDE / RL_GUIDE; **(10)** only with an actual topology change: driver / bridge per-joint table, reflex / gestures / keyframes beyond (N, 3), duty = 1 - 1/n, a generated RL obs builder, per-leg overrides, the deck and shell as an n-gon. |

## Session-9h additions (2026-09-25, D055)

| B37 | One capability registry (D056) | **OPEN, next** | Tools are defined in four places (harness/local_brain.py schema, sim/cockpit_brains.py EXTRA_TOOLS, harness/server.py MCP, harness/cockpit_backend.py forwarders); the MCP reads gestures and chord words once at start. Design: `harness/capabilities.py` registry (name, kind, description, schema with live enums, annotations, gated flag, executor) + the physical envelope from `RobotSpec`/params in every description; cockpit `GET /api/capabilities` with a version hash + an SSE event; generate the local-brain schema, the MCP tool list (rebuilt on every `tools/list`, `send_tool_list_changed` when the hash moves — the venv's mcp SDK has add_tool/remove_tool/list_tools/ToolListChangedNotification), the executor table and `docs/TOOLS.md` (CI checks it is current, like the MJCF). Add a gesture, a chord, a tool or a joint and every client sees it at once. |
| B38 | Place recognition: "basement at home" / "somewhere new" / "the bedroom has a new chair" | **STEP 1 SHIPPED (D057), OPEN beyond it** | Shipped: `sim/place_memory.py` recognises a place from the senses, not the world's name: a scan signature (nearest return per 10° of map heading at the best rotation + the sorted range histogram), a look-description embedding (llama-swap's CPU `embedding` model), a radio fingerprint slot; one confidence per place, verdicts known / new / ambiguous (+ unknown with no signal), and no single sense can say known (`SINGLE_EVIDENCE`: a lone sense stops at 0.725); `diff_objects` + `confirm_diff` = change detection that needs a second look from another angle and claims nothing about spots the eye could not see; `places.json`. The cockpit runs it behind `--recognize` / `POST /api/awareness {recognize: true}`: the scene memory keyed by the recognised place, the situation line's place clause, the four place tools (`where_am_i` / `name_place` / `places` / `forget_place`) offered to the models, and by the MCP server over a cockpit, only while it is on, naming as a correction of a wrong recognition (`rename=true` to rename), "this X has a new Y" and a world edit as two-look change checks of the bound place, talk mode's place sentences, `/api/place`. `sim/place_bench.py` scores the three verdicts from the senses alone (opaque world names, a name-swap case). **First run** (2026-09-25, lfm2.5-vl, 3 trials, `sim/out/place_bench.json`): 15/21 correct, 0 wrong "known", the never-seen room new 3/3 (0.26–0.29), the true matches at the threshold (known 0.755–1.00, ambiguous 0.67–0.735: a scan of 0.87–1.00 pulled down by a description part of 0.26–0.52), changes 0/6 (trials 2–3: the changed rooms were ambiguous, so no check ran; trial 1: the check read objects out of the look's prose, which gave no metres); `docs/SIM_GUIDE.md` §3b. This round, after it and not re-measured: the description map re-set from the run's records (0.25 / 0.90: replayed, all 18 revisits known; the description does not tell these rooms apart, it only stops counting against the right one), the change check moved to the eye's box queries (`/api/look {find, method: bbox}` per name), a new place stored from two looks. Remaining, in order: **(a)** the hierarchy home → room → spot (`parent` is reserved; a spot is a scan sample, a home is a radio cluster); **(b)** the radio fingerprint on the real robot (its Wi-Fi scan; the sim never makes one); **(c)** a switch for recognition and the place on the cockpit page (`sim/cockpit_ui.html` has neither); **(d)** the real camera and the real puck (every number so far is clean MuJoCo or synthetic, an upper bound), with a translation between session map frames (`scan_align` gives the yaw only) so a real robot's object positions compare across visits. **After the second bench (21:15, 21/21 verdicts, changes 3/6):** (h) an ADDED object is never confirmed — the eye names the planted chair a box and, asked again from the same spot after the 30° turn, denies it; fix = the confirming look from a sidestep (a different viewpoint) and name folding (chair ~ box ~ cube) in checked_objects; (i) the never-seen room is new by only 0.01–0.06 over its runner-up — widen the margin (a second description model, or the radio on the real robot); (j) a room that shares another's walls is ambiguous at enrolment until named (by design, but say so in the situation line). |
| B39 | Who is speaking: speaker embedding, people memory, trusted-voice rule | **OPEN** | A speaker-embedding model (~20 MB, ~50 ms CPU) on the same clip whisper-fast gets; enrol from the cockpit; each utterance gets a name + confidence; known operators may command motion, unknown voices get chords and conversation (stop works for anyone); a people collection in the memory (name, voice vector, preferences, last seen, a bounded chat summary) and a situation-line clause ("Tyler is here; last time he asked me to find the ball"); everything local, explicit enrolment, `forget` applies. |
| B40 | Sensors as data: lidar / ToF / depth camera in params `sensing`, an occupancy + height map in the memory | **OPEN** | `sim/sim_lidar.py` already fakes a 360° 2D puck with mj_ray fans, a legged-odom EKF and the LaserScan contract. Promote it to a first-class sensor in `cad/params.yaml` (mount height, rays, rate, range, min range, noise, the legs' occlusion), add a downward ToF grid per foot (VL53L5CX/L8CX class: sees the edge before the foot does) and a depth-camera model on the eye (a depth render, free in MuJoCo); feed all three into a per-place occupancy/height map. Real-robot order: 2D lidar on top + ToF at the feet, a depth camera with onboard compute, 3D lidar only on a bigger platform. |
| B41 | Gemma as a brain: thinking on, a Gemma-style prompt, re-bench | **SHIPPED (prompt notes measured 2026-09-25: Gemma 12B 20/20, Qwen 9B 19/20; thinking hurts)** | Both Gemma 4 models answered a bare "stop" with a chord and the 26B gave up on compose_gesture; the stop gate covers the hazard, but the owner trusts the family. A/B: `reasoning_effort` / thinking on for the 12B (latency cost), a prompt written the way Gemma's card recommends (explicit numbered steps, tool-first), the MTP drafter on disk (untested with --mmproj). Judge by the same brain_bench rows. |
