# Decision Log (ADR-lite)

*One line of context, the decision, and why. Revisit only with new evidence.*

| # | Date | Decision | Why / consequence |
|---|---|---|---|
| D001 | 07-27 | **Subscale first** ("Pebble", 1:3) before GSD-scale Rocky | Torque scales ~L⁴; mistakes cost $17/joint not $500. Software transfers. |
| D002 | 07-27 | **ST3215 12 V (30 kg·cm) only**, 3S pack | 7.4 V variant fails the 3-leg-stance torque budget. Order carefully — listings mix variants. |
| D003 | 07-27 | **One serial bus, no per-leg MCUs** at subscale | 1 Mbps handles 20 servos at 50–100 Hz; five ESP32s = complexity with no payoff. Full-scale Rocky revisits (CAN + leg nodes). |
| D004 | 07-28 | **Code-CAD (build123d), `params.yaml` = single source of truth** | Tyler has no CAD background; parts as Python + one param file shared with the future URDF. Claude iterates, Tyler reviews renders/viewer. |
| D005 | 07-28 | **True radial symmetry + transforming 3-finger hands** | Rocky-canon. Closed fingers form the cone foot → grasp servo carries no walking load. |
| D006 | 07-28 | **Creature look = bolt-on shells over a pure-mechanism skeleton** | Iterate aesthetics freely without touching working joints. Rock texture is a late cosmetic pass. |
| D007 | 07-28 | **Interactive deliverables: self-contained HTML, browser-verified before delivery** | Chat preview sandbox blocks CDNs (three.js failure). Hand-rolled WebGL viewer; Playwright/Chromium screenshot check mandatory. |
| D008 | 07-28 | **Gait defaults respect CAD joint sweeps** (1.6 s cycle, R0 185, 45 mm/s → coxa ≤ 22°, validated ±40°) | Software limits mirror verified mechanical limits; enforce in firmware + URDF later. |
| D009 | 07-27 | **ROS 2 Jazzy + MuJoCo sim-first**; gait core stays dependency-free Python | Walk in sim before hardware exists; same module drives sim, then ros2_control. |
| D010 | 07-28 | **SEA cartridge sits between tube and hand hub**; slider stub = tube-OD | Hand hub needs no redesign; spring travel 7 mm; microswitch = free contact sensor. |
| D011 | 07-28 | **First prints in PLA**, even parts that end up PETG | Material is sunk-cost-free iteration; dial fit before spending the purple. |
| D012 | 07-28 | **Deck = R100 one-piece + 25 mm coxa cantilever**, not a split plate | 289 mm plate can't fit the Prusa bed; cantilever moment is negligible. |
| D013 | 07-28 | **Default manipulation stance uses NON-ADJACENT arms** (0 & 2) | From nominal stance, adjacent-arm CoM sits ~57 mm outside support. AMENDED by D018. |
| D014 | 07-28 | **Cross-section slices are a standard CAD verification** alongside renders + booleans | Caught hand v0.1 wedges that were near-full cones (fingers could never nest). |
| D015 | 07-29 | **Batch 1 servos stay ST3215 12 V; STS3250 (50 kg·cm, same case) is the sanctioned upgrade path**, optional 2× trial via AliExpress for Batch-2 femurs | Walking margins fine on ST3215 (22–33% stall); STS3250 takes untucked 3-leg manip from 43–65% (thermal no-go) to 28–42%. ST3235 rejected: 30 kg·cm despite the name, 2.3× price, durability-only. Hiwonder (proprietary bus, pot sensors) and Dynamixel (½ the torque/$) rejected. Caveats to bench: STS3250 spec 6–12 V vs 12.6 V full 3S; 4.2 A stall. |
| D016 | 07-29 | **Hand micro-servos get their own 5–6 V power domain** (BEC in Batch 2); TTL data + GND shared with the 12 V leg bus | SCS0009 is a 4.8–6 V device — the master-plan diagram had it on the 12 V rail, which would cook it. Also: SCS speaks protocol 0, STS protocol 1 — driver must handle both on one bus. |
| D017 | 07-29 | **Sim robustness baselines recorded** (blind gait, zero reflexes): rubble ≤30 mm crossed reliably, snag-not-fall to 40 mm (step height 32 mm is the binding limit); push envelope 35–53 N walking / 46–53 N stance (0.15 s shove; ≥1.3× bodyweight) | Phase-3 terrain adaptation should trigger on progress/contact-timing, not tilt (it never falls, it stalls); raising step height is the cheap terrain lever; stabilization is for grace, not survival. |
| D018 | 07-29 | **Adjacent-arm manipulation is feasible via foot repositioning** (amends D013): crouch 30 mm → step flanks to R280 at ±22° (coxa ≤39.8°) → lean 12 mm → FOLDED carry pose | Sim-verified: tilt ≤5.1°, work margin +10 mm (control case falls at arms-up, as D013 predicted). Raised-limb mass shifts CoM ~20–35 mm — reach-out arm pose tips the robot; carry pose is mandatory. Margin is thin: hardware version wants contact confirmation before trusting it. |
| D019 | 07-29 | **Two-arm mode selection: NON-ADJACENT (0&2, leg between) is the default for all two-handed work; ADJACENT (D018 recipe) is reserved for cooperative bimanual** | Workspace math at coxa ±40°: non-adjacent hands can never close within ~252 mm (disjoint reachable sets — independent tasks only); adjacent hands close to ~94 mm — the only cooperative config. Middle planted leg never obstructs (both hands ≥32° off the bisector). Bimanual levers if wanted later: coxa range (clevis v2), finger length, wrist DOF at full scale. |
| D020 | 07-30 | **Six standard interfaces, spec'd + frozen** (`docs/INTERFACES.md`, dims in params.yaml `interfaces:`): I1 leg port (dowels + hook-pivot + captive thumbscrews; XT30 + JST-XH-5), I2 tool socket (quarter-turn bayonet on the SEA stub + JST-SH-3), I3 panel standard (replaceable quarter-turn latch inserts + magnet pockets; belly door + top hatch first), I4 avionics tray (rails + one latch + rear bulkhead), I5 battery sled + XT60 blind-mate dock + XT30 spare taps, I6 accessory dovetail ring. **Rule: loads route through dowels/lips/shoulders, never through latches/screws/magnets.** | Serviceability is a design requirement, not a nicety: leg swap <2 min, tray out with one latch, panels in seconds, tools quarter-turn. Each interface has an explicit Rocky-scale mapping (steel dowels/cam levers, powered tool flange, SB120, rail ring) so Pebble validates the architecture, not just the parts. |
| D021 | 07-30 | **Hand v0.2.1: design open = 55° (was 65°), cam slots carved from the true tilted-pin slab footprint, OPEN-ENDED at full travel; pin arm raised 3 mm** | Dense pose sweep (5° steps — the 3-pose check missed it, D014 again) found the residual graze was the PIN leaving its slot: a tilted pin's slab footprint grows ~1/cosθ + T·tanθ; at 65° the needed slot breaches the cam rim AND the skirt clips neighboring lugs. 55° was already the software cap (07-28) — geometry and software now agree. Full-open end stop = servo limit, not the cam. Worst clash across 14 poses: 0.00 mm³. |
| D022 | 07-30 | **Push-recovery reflex (crouch-and-brace on \|gyro\|>1.8 rad/s) adopted for WALKING only; quiet stance stays stiff-planted** | Sim: walking floor rises 33→40 N (≥1.5× bodyweight all directions) and the weak gait phases flatten; in stance the brace transient *costs* ~6% (stiff+planted is already optimal). v1 lessons encoded: never slide loaded feet, ramp the crouch (a step-crouch unloads feet mid-shove), plant the swing leg where it is. `gait/pebble_reflex.py`, results `sim/push_reflex_results.json`. |
| D023 | 07-30 | **Stuck watchdog (progress ratio < 0.4 for 2 checks → escalating retry: step 46→52 mm, slow, +body, reverse) adopted; ramped shin fairing REJECTED by sim** | 40 mm rubble crossings 1/4 → 4/4 with the watchdog (D017's "trigger on progress" was right). Fairing (`run_fairing.py`): helps only at 45 mm (outside the envelope), *hurts* the watchdog at 40 mm (fat shin closes the gaps high-stepping threads through) — don't print it. 45 mm+ remains out of scope for Pebble. |
| D024 | 07-30 | **360° lidar promoted to Phase-3 CORE via a used LDS02RR vacuum pull (~$17, maintained ROS2 driver); scan contract frozen now** (`sim/laserscan_spec.json`, sim-lidar + occupancy demo working) | The $99 optional line is dead: pulls cost 1/6 of it and the SLAM pipeline can be built entirely in sim before the puck exists (it was, this session). Neato XV-11 pulls aged into ~$60 collector stock — wrong pull. No-gamble alt: LD19 new at ~$90 (and 42 g vs 190 g — the mass audit's lever). |
| D025 | 07-31 | **Push reflex DEMOTED to a safe-stop/posture primitive; the bare wave gait is the best single-impulse controller.** v2 (`pebble_reflex.py`: finish-the-step PLANT + tilt-vector contact-seeking BRACE) replaces v1 wherever a brace is wanted; reflex stays OFF for push survival. | The D022 numbers were PHASE-CONFOUNDED: the supervisor's gait clock ran during idle, so every reflex trial was 0.625 cycles offset from its baseline. Phase-aligned re-measurement (`run_push_reflex_v2.py`): walk envelope mean base 43.5 N > v2 40.9 > v1 37.1; v2 ≥ both at every probe phase in the weakest direction (v1's +0.25T pocket erased); sustained-lean test shows no reflex benefit either (gyro trip barely fires on a slow lean). Mechanism findings that survive: freezing mid-swing leaves a broken polygon at the shove peak, and crouching loaded pivot-side legs during a tip actively pushes the robot over (51° vs 14.7° tilt). PLANT→BRACE (freeze only at swing-low + ≥3 contacts) is the correct EMERGENCY STOP — pairs with the cliff detector. |
| D026 | 07-31 | **EKF yaw observability: standing zero-yaw-rate pseudo-updates via a StillnessGate (1.2 s quiet + full contact); gyro-bias prior opened to (0.01 rad/s)²; mag stub optional-off.** | Yaw drifted at exactly gyro-z-bias rate (~6°/21 s) — nothing in IMU+legs observes yaw. The naive fix fired during the boot settle and learned the WRONG bias: a position-servo robot keeps a decaying real twist (0.3–1.2°/s — bias-scale!) for >1 s after any pose change; calibrate at the END of pauses, never the start. Results: flat walk 6.8°→0.25° end yaw, patrol 15.1°→1.7°, bias estimate lands on truth, translational drift also improves (10.8→5.5 %). |
| D027 | 07-31 | **SLAM front-end owned in-repo: EKF-seeded point-to-line ICP with keyframes** (`perception/scan_matching.py`, `sim/run_slam_lite.py`); slam_toolbox stays the ROS 2 target but nothing waits on it. | Lap metrics: trajectory ATE 55.6→8.2 mm RMSE, end 75→10 mm, yaw 4.8°→0.06°, map IoU vs truth 0.26→0.72 (17 keyframes, 2.8 mm median scan fit). `sim_lidar.py` now runs the EKF in-loop so the archive carries real odometry. Bonus find: the session-4 lap was physically grinding a pillar at 43 % speed (GT poses mapped fine so nobody noticed; the EKF noticed) — lap routes are now clearance-audited. |
| D028 | 07-31 | **Chord-speak v0.2 voice spec ("Eridian"): chord-SYLLABLES with speech envelopes; just-intonation stacks with septimal/undecimal intervals on 66–200 Hz roots; one fixed formant throat + band-limited breath; roughness/detune = urgency/uncertainty. NO scale runs, NO cadences.** | Tyler's verdict on v0: "too musical, too nice." Measured fix: 12-TET pitch-grid energy 85 %→24–39 % (amaze keeps 72 % deliberately), spectral centroid ~7 kHz→210–360 Hz after band-limiting the breath (the first v2 render was a whisper, not a chest). Event engine (`chordspeak_events.py`) narrates runtime events with priorities/cooldowns/preemption; the 40 mm stuck-retry video now carries its own narration. v0 kept in-repo for A/B. |
| D029 | 07-31 | **Carapace v0 architecture: five IDENTICAL printable rock sectors + hatch cap** (`part_shell.py`): terraced perturbed-pentagon tiers, leg arches sized from the fork-swing disc (the entire yaw sweep stays within ±14° of the station — arch ±22° covers it), web feet (r 73–82, az 21–36°) with latch pad + magnets, vertical I6 bars at ±27°, B8 LED groove FOLLOWING the rocky wall, B9 gills. All four keep-outs boolean-clean. | Pentagon math bites: the soft-pentagon factor shrinks mid-edge radii ~11 % — every wall-relative placement must query the outline function (a circular LED groove missed the wall entirely at the webs). Mass flag: 39 cm³/sector ≈ 47 g PETG ×5 — a thinning pass rides the post-caliper regen. Latch inserts (Ø14.3) outgrow the 9 mm foot annulus → local pads, placed where the coxa-plate y-clearance is generous. |
| D030 | 07-31 | **Carapace seams are REAL joints: 72°-periodic outline noise + a chaining tongue-and-groove on the sector edges; deck v0.4 answers the shell (5 latch strikes with tangential peg entries, 5 washer recesses, power grommet); shell amended to ONE magnet per web at r 76.** | Non-periodic rock noise meant five identical sectors met with mismatched wall radii at every seam — invisible in renders (disjoint wedges can't interfere), fatal for any edge joint. Tiling one 8-vertex noise pattern 5× makes seams flush by construction; the vertical tongue/groove then registers neighbors (pair interference 0.00 mm³, engagement check 15.5 mm³ displaced — and the first groove was cut into open air on the wrong side of the edge plane, caught by that check). Deck side: radial peg slots breached the pentagon edge by 0.9 mm (audit caught it) → tangential entries. The 3-magnet web layout died because its washer recesses don't fit on the deck. |
| D031 | 07-31 | **RL kit v1.1: per-episode DR draws from stored base values (v1 friction randomization COMPOUNDED across resets — a random walk, not a draw), + mass/torque/gravity-tilt/action-latency randomization, per-episode command sampling (`--cmd-sample`), yaw-rate tracking in the reward.** First real training run launched in-session. | The compounding-friction bug means session-4's smoke ran on drifting physics. v1.1 draws each episode from clean bases. Command sampling turns the residual policy into one conditioned controller for the whole (vx,vy,wz) envelope — the command was already in obs. In-session run (4 async envs, ~1000 sps on 2 container cores): episodic return 39 → 74+ by ~500k steps under full DR — the kit demonstrably learns; the laptop overnight run is `train_ppo.py --num-envs 8 --cmd-sample`. |
| D032 | 08-07 | **Measured fit numbers have TWO sanctioned application paths: params regen (canonical) and slicer XY-hole compensation (weekend fallback).** The GO/NO-GO table in `docs/PRINT_WEEKEND_s6.html` maps fit-ladder outcomes to a PrusaSlicer hole-compensation value so a print weekend proceeds without a Claude session; the numbers STILL get filed in NOTES_INBOX and baked into params at the next regen. | Session 6 status: printer fixed, nothing printed, nothing ordered — the constraint is Tyler's weekend, not CAD. A +0.05–0.10 mm hole compensation is the same physical correction as a params bump for BORES; it is NOT equivalent for pegs/slots/walls, which is why regen stays canonical and the fallback is explicitly bores-only. |
| D033 | 08-07 | **Plate-layout audits (edge margin + feature-to-feature min-wall) are mandatory module checks for any multi-feature coupon/plate.** `part_fit_ladder.py` now asserts ≥1.5 mm edge margin and ≥1.0 mm wall between all features incl. slots. | The v1 fit ladder SHIPPED with its Ø4.8 heat-set pocket merged into a lip slot (wall broken open → that pocket would read falsely loose). Found in session 6 by a section-slice pass; renders never showed it — the D014 lesson (slices + booleans over renders) applies to flat plates too. |
| D034 | 08-07 | **The cliff detector's stop path IS the safe-stop primitive: `ReflexSupervisor.request_stop()` routes any external halt (VOID, operator stop, watchdog) through PLANT→BRACE→RECOVER-to-idle.** Adopted after `sim/run_cliff_safestop.py` measured it against the old path. | The old path (retreat → command v=0) leaves the bare WaveGait MARCHING IN PLACE at the cliff edge — 24 post-halt contact breaks in 4 s, feet lifting one at a time next to a void. With request_stop: **0 contact breaks**, same stop margin (185 vs 188 mm), lower peak tilt (0.49° vs 0.68°), and the detector still fires with the supervisor owning ctrl. Same primitive now serves the MCP `stop()` tool. |
| D035 | 08-07 | **Expressive gesture poses are authored in JOINT space, inside the real joint ranges — never as position waypoints IK'd near the limits.** | The beckon v1 curl (60 mm position-space) IK'd to hip 98–117° against a +90° joint limit: both curl extremes clamped to nearly the same pose and the rendered arm DIDN'T MOVE (caught by comparing extreme frames — the render looked plausible in stills). Bonus find: fist-bump's _CARRY/_EXTEND waypoints also IK past the hip limit — the shipped video rides the clamp benignly, but hand-v0.3-era retuning should re-author them in joint space too. |
| D036 | 08-30 | **Single-solid connectivity audits are mandatory module checks for every printable part: assert the built Part is exactly ONE solid (`len(part.solids()) == 1`) plus a finite-volume attachment probe at each joined feature; module `__main__` now exits nonzero on any failed check so `run_all_checks.py` actually enforces it.** First applied in `part_hand.py` (hub/cam/finger). | The v0.2 hand hub SHIPPED (v0.7.3→v0.7.6, two releases) with all six hinge lugs FLOATING 4.3 mm above the hub top — six 146 mm³ islands in the slicer, found by Tyler in session 7. Mechanism: v0.2 raised KNUCKLE_Z 4.2 mm to clear the cam disc (the v0.1 clash fix) but nobody re-attached the lugs, and v0.1's lugs were themselves 0.1 mm proud of the hub — they were NEVER attached. Interference checks are blind to this failure class by construction: they detect overlap, and a disconnection is the absence of overlap — "CLEAN" was the bug. Fix: knuckle collar (annular wall r 19.45–22, hub top → lug top, outboard of the cam's swept rim) with swept tab-envelope windows at the stations (the finger tab's upper corner arcs to r≈20.8 mid-open — same by-construction sweep trick as the finger scallops, in reverse); lugs widened to reach it; pin bores lengthened to pass through the collar so pins insert from outside and the far wall retains them. Hub = 1 watertight solid, cam×hub 0.00 mm³, worst finger clash over the 14-pose dense sweep 0.23 mm³, tree CI 20/20. |
| D037 | 08-30 | **The D036 single-solid gate lives in `common.export()` itself: every export raises unless the Part is exactly one solid, and only assemblies/posed previews may pass `multi=True` (explicit, at the call site).** Tree CI 20/20 with the gate live; mesh-level audit of all 66 STLs confirms zero printables with more than one body. | Session 8's first tree-wide audit found the hand hub was NOT a one-off: SIX printable parts were shipping as disconnected bodies — `tool_scoop` (bowl, neck and socket never touched, three pieces), `coxa_fork` (the −X high-collar wall attached to nothing, 0.3 mm from the back wall), `shell_sector` (both web feet + latch pad + magnet floating 13 mm inboard of the skirt), `stand_section` (skirt 0.3 mm from its own tube), `battery_sled` (finger scallop cut the floor through; tail lip severed, touching along one tangent line), `jig_column` (deck-proxy plate 6 mm short of the spine). Plus `fit_ladder`'s row-D index dots engraved inside the slots (non-manifold seam). Every one passed its own interference checks. Putting the gate at the single chokepoint all modules use means no module can forget it, and `multi=True` makes intent auditable by grep. Three of the bugs (fork, stand, scoop-scale gaps) were exactly one `clearance_fit`: a part placed against ANOTHER part's envelope instead of its own — that's the pattern to watch for. |
| D038 | 08-30 | **Print-physics audit is a mandatory post-stage of the CAD CI: `cad/check_printability.py` slices every printable STL in its PRESCRIBED print orientation (registry in the module) and fails on (a) any island where the plan says "no support", (b) the part floating above the bed (an island >25 % of the largest layer with nothing under it), (c) a wall segment thinner than 0.8 mm that persists ≥1 mm in height; it reports support islands (bed/part), overhang steps and <1.6 mm walls as warnings the print plan must answer.** `run_all_checks.py` runs it after all 20 modules export. | The D036/D037 gate proved connectivity but lives in CAD space; the build plate has its own physics. First run over 51 parts: `coxa_fork` ("hub down, no support") had its whole floor plate 2.5 mm above the bed on a Ø26 hub — AND, found by the probe that followed, the floor had covered all four yaw-horn screw holes since v0.1: the fork could never be bolted to its servo (fixed: floor to the horn plane, Ø4.2 counterbored access, pattern rotated 45° to clear the collar wall; new in-module checks: yaw sweep ±40°, femur-servo fit, driver-column access). `tibia_sea_outer`: 0.8 mm skin behind the slider keyways (ribs added). `shell_sector`: the four tier cavities were separate solid prisms, so every tier roof was a FULL DISC — a carapace of sealed chambers with 2.4 mm floors spanning 80–100 mm (unprintable bridges, no support removal, no LED wire route; 70 % of the sector's mass): now one connected cavity with 45° cone ceilings (41 → 24 cm³/sector); plus the LED groove's 0.4 mm skin, the feed notch 0.8 mm from the seam plane and the outer gill 0.5 mm from it. `dock_base`: the approach ramp dipped 2.4 mm under the floor plane — the plate floated on a knife-edge. `jig_column`: two 84 mm bridges on its back (two gussets added). `coxa_yaw_base`: the plan said "support under the crown arm only" — the I1 hook lip holds the plate 9.5 mm off the bed. `servo_blank`/`calib_gauge_hip`: the prescribed lying orientations island a face; both print upright. Semantics learned the hard way: 45° feathers (a plane grazing a cone/cylinder) and 0.2 mm² bore break-outs are not walls — hence the persistence rule and the per-blob threshold; the gate was regression-tested against the pre-fix STLs (all four known-bad parts still fail). |
| D039 | 08-30 | **Sim masses come from the CAD tree: `sim/mass_audit.py` (STL volume × material × print_estimate fill factor + `params.mass_hw`) writes `sim/mass_budget.json`, which `build_mjcf.py` reads (session-2 hand budget stays as the fallback). Re-baselined D017 on the refreshed model: push envelope is unchanged in BODYWEIGHT units (walking min 1.17 → 1.19 BW, stance 1.54 → 1.54 BW); absolute newtons fell ~13 % because the robot is 13 % lighter (3.05 → 2.65 kg est.); rubble (25–40 mm) and walk metrics identical.** | The budget had the legs heavy and the torso light (coxa 140 → 86 g, femur 30 → 16, tibia 170 → 135, torso 1350 → 1462 g). Same scripts, same seeds, same ladders — only the masses moved, so the comparison is honest. Every `mass_hw` value is VERIFY: bench day's kitchen scale replaces this whole file (NOTES_INBOX → params → regen). Quote push numbers in bodyweights from now on — that is the invariant. |
| D040 | 08-30 | **Gesture library v2 (`gait/pebble_gestures2.py`: wave, bow, look_around, shake, sit, turn_in_place, sidestep) ships with a static-stability assertion in its self-test (body-origin projection ≥15 mm inside the planted-foot polygon) and a physics harness (`sim/run_gestures2.py`) that must show each gesture's signature metric actually happened (bow pitches 4.5°, sit drops 54 of 56 mm, look-around peaks at its yaw, turn yaws 80°, sidestep travels 170 mm) — the D035 rule, mechanized.** Registered in playground and the MCP harness (`gesture` tool now lists 10). | Found while authoring: body yaw with planted feet costs ~2× at the coxa (feet at R185 about a coxa at R100) — a 22° look would have needed 45° of coxa; 16° body = 33° coxa. Static margin of a one-arm pose is 57 mm, all-planted 150 mm (the pentagon apothem). All seven: 0 unreachable targets, 0 planted-foot breaks (shake: 0.18 s of micro-lifts at 5.5 Hz, accepted), tilt ≤ 4.5°. |
| D041 | 09-01 | **Recovery reward v2: success IS the handoff criterion** (`RecoverEnv(reward="v2")`: tilt < 25° AND h > 0.09 m held 0.5 s terminates with the bonus; +0.2·feet/5 pays only while upright; standing bonus 2.0). Constants exported (`HANDOFF_TILT/H/HOLD_S`) and shared by eval_recover and the reflex FALLEN branch — one criterion, three call sites. v1 stays selectable for reproducing recover1. | v1's success (tilt<20°, h>0.10, ≥4 feet, 1.0 s) never once fired in 2 M training steps (ep_len pinned at 300) — the policy was optimizing dense shaping toward a bar it couldn't reach, and learned to prop on ~1.5 feet because feet paid nothing. Train the criterion you'll actually hand off at, not a prettier one. |
| D042 | 09-01 | **ReflexSupervisor grows FALLEN and RIGHTED states** (`gait/pebble_reflex.py`): tilt > 60° held 1.0 s → FALLEN (a pluggable `righter` callable — the recovery policy — commands absolute joint targets; supervisor stays torch/sim-free for the Pi); D041 handoff criterion held 0.5 s (or a 10 s deadline) → RIGHTED (smoothstep joint-space ramp to the planted stance, 0.6 s + 0.5 s hold) → NORMAL. Re-fall during the ramp re-enters FALLEN. 6 pure-Python state-machine tests + `sim/run_reflex_fallen.py` end-to-end. | The full loop closed on the first end-to-end run: 120 N shove (3.4× the D017 walking floor) → brace fires and loses → tumble → FALLEN → policy rights it → walks away, **5/5 seeds** (one via the deadline ramp). The 1.0 s confirm matters: braces routinely spike tilt past 60° transiently and must not surrender to the righter mid-save. PolicyRighter adapter reproduces RecoverEnv's obs/normalization/5 rad/s rate limit exactly — the policy must see the dynamics it trained on. |
| D043 | 09-01 | **scan_summary is live in every sim world** (`SimBackend`: the sim_lidar ray fan, 8 map-frame sectors + nearest + frontiers) **and a new `ROCKY_WORLD=room` binds the sim_lidar room for patrols; the result carries its own honesty note: a 2D horizontal scan cannot see voids — cliff safety remains the foot-contact guard.** `sim/run_patrol.py`: first mission built purely on the tool surface (goto/scan/say), 5/5 waypoints, chord-speak report, JSON artifact. | Replaces the "no lidar in the cliff world" refusal with a sensor that exists AND tells you what it can't do — the absent-tool>lying-tool rule, third form: a present tool that documents its blind spot. Patrol scans cross-check geometry (north wall at 0.737 m from y=0.55 — exact). |
| D044 | 09-01 | **Torque re-audit on D039 masses (`sim/torque_audit.py`): the servo-sizing driver is now SELF-RIGHTING, not manipulation.** Robot 2840 g. Walking 12–16 % of ST3215 stall; untucked-carry hip 17 %; the one WARM number is the recovery knee push (W/2 through one crouched leg): 1.47 N·m = 50 % ST3215 / 30 % STS3250. Buy recommendation (order sheet v2, prices re-verified 09-01): 10× ST3215 (yaw/hip) + 5× STS3250 knees + spare, ≈ $585; all-ST3215 remains a legal $435 budget path. ST3215-HS rejected (20 kg·cm = torque downgrade). | D015's scary 43–65 % manipulation case evaporated with real masses (femur is 15.7 g, not 30 g with the old torso). Static holding torques only — dynamics ride on top, bench day re-baselines (D017). STS3250 caveats stand: 6–12 V spec vs 12.6 V full 3S, 4.2 A stall ×2 simultaneous knees for the fuse math. |
| D045 | 09-01 | **recover1 remains the shipped righter — the v2 retrain is a recorded NEGATIVE result with a diagnosed cause and a shipped fix.** Final numbers (same 20 seeds, deterministic): recover1 12/20 stood-after-handoff (re-verified today) vs recover2@3.67M 2/20 (barely above hold-pose's 2/20); in the FALLEN showcase recover2 stood 5/5 but every stand came from the 10 s deadline ramp, while recover1 handed off properly in 3/5 and walked ~0.55 m after vs ~0.2. `run_reflex_fallen.default_ckpt` prefers recover1. Cause: with clip(±1) + the 5 rad/s rate limit, WIDE actions are a strategy (saturated samples = bang-bang) — sigma inflated past 1.0 and kept rising even with ent_coef 0 (13.8 → 22.1 nats across the run), so the deterministic mean decayed while stochastic training returns climbed (~610). Fix shipped: `--log-std-max` cap in train_ppo (e.g. −0.5); the capped v2 retrain is the queued laptop experiment. | Three lessons at once: (1) training-time success rate ≠ deployable policy when sigma is a controller; (2) removing the entropy bonus does NOT stop sigma growth here — the policy gradient itself inflates it through the clip; (3) eval the MEAN early (the 2.58M interim eval caught this with 1.9M steps still to burn). The v2 reward idea (success = handoff criterion) is still right — it produced training-time terminations v1 never had — it just needs the cap under it. |
| D046 | 09-17 | **The leg chain is rebuilt around its five JOINTS, and a joint suite (`cad/check_assembly.py`) is a CI module: for every joint it asserts the assembly ORDER (each part arrives along an open path; driver columns clear of everything present when the screw is driven), alignment across both mating parts (coaxial bores, castellation fit), the thread/nut path, and capture (a 2 mm nudge meets material or is a declared bolted direction). Concretely: the crown arm is a bolt-on cap over an M3 axle (printed stub gone); the femur link takes the horn coupler on both hubs (never touches a horn); coupler lobes moved off their own M2 heads and the recess clamp angles are mirrored; lips + straps replace zip ties in every cradle (`servo_mount.py`); blank v0.2 (Ø14 undercut fill, nut slots); four joint COUPONS that are boolean clips of the production solids, printed before any full part. Deck, shell and I1–I6 untouched. The yaw joint deliberately has no coupler (clamp heads would sit under the cap).** | Tyler's first bench dry-fit (laptop, 2026-09-17): the femur link attached to nothing, the blanks did not attach, the coxa stage "wasn't great" and nothing past it connected. Probes on the v0.8.0 tree: all four yaw-horn screws sat under the crown arm (45 mm³ of base in every driver column — the in-module check had only tested the fork), the pre-bolted fork could not slide in either (stub vs arm), so NO assembly order existed; the vertical budget was 0.5 mm; each femur hub was 4x M2 into a 3 mm PLA disc then 3 mm of air, no location; every blank was zip-tie-only; the coupler (§3.4) was an orphan. D036–D038 proved non-overlap, single-solid and print physics — none asked whether two parts could be brought together or what held them. Same failure class, fourth appearance; the fix is the missing check class, not another part fix. |
| D047 | 09-22 | **The leg chain is rebuilt around the REAL servo, and the servo model is derived from a measured STEP, not a datasheet box.** `cad/ref/STS3215_03a.step` (SO-ARM100, Apache-2.0) was ray-cast and probed; `params.servo_st3215` v2 carries the results (case 28.8 flat-to-flat with 1.5 mm rims and their four screw holes per face, 2.6 mm gearbox plateau, axis 10.2 from the front end, horn Ø20 × 2.5 with its top at 33.1, a 45° hole pattern, a Ø19.9 rear idler, the connector housing and pins). On that servo: the yaw servo sits SHAFT-DOWN in a cup on the I1 base (cables come out on top); one C-shaped fork rides the horn below and the idler above (36 mm couple — no 683ZZ, no cap, no axle); the femur is twin-plate (plate A on the couplers, plate B on the idlers, bridged and bolted); the knee servo sits in a cup on the tibia; every servo is held by four self-tappers in its own rim holes (`servo_mount.servo_cup`, one shape for all three) instead of lips + straps; the coupler's lobes return to 0/90/180/270 between the real 45° screws and its holes are radial SLOTS with counterbores (M3, or M2 + washer); the cable plugs are a keep-out solid every part is asserted clear of; the hip axis is `params.leg.hip_axis_z` = 61 (gait, MJCF and URDF read it — three files had 58 hard-coded). Four coupons (cup, yaw hub, hip hub, idler tower) are clips of the production solids. Deck, shell, I1–I6 untouched. | The 2026-09-22 review measured the STEP against `servo_st3215.py` v0.1: the box was wrong in five independent ways (height, axis position, horn height, hole pattern, and it omitted the idler, rims and connectors), so every D046 leg part — including the ones in the 09-17 photo — was designed for a servo that does not exist, and the D046 yaw stage put ~240 N through a 683ZZ 15 mm from the horn in a recovery push (bearing clamped solid by the axle head, 0.9 mm boss walls). Two-sided joints on the idler are how every twin-plate hexapod kit uses this servo class and cost nothing: Feetech ships the idler in the box. What the STEP could not settle (horn hole radius 14–15.6 BCD, horn thread) is hedged in the two cheapest parts, which are the first coupons. CI 23/23; joint suite clean with four new check classes; mass 2665 g; sim walk unchanged; URDF parity restored (masses now from `mass_budget.json`). |
| D048 | 09-23 | **Shoves are modelled as a hand shove, not a strike: `sim/shove.py` — a half-sine force at the carapace's top rim (60 mm above the torso frame), applied to MuJoCo as the force at the CoM plus the torque the lever makes, reported in N·s and bodyweights.** The playground's `push`, `run_reflex_fallen.py` (40 N peak × 0.4 s = 10 N·s, 1.5 BW) and the new `shove_envelope.py` use it; the D017/D025 push-envelope scripts keep their rectangular CoM pulse because their JSONs are decision records. Same commit: BRACE holds its foot targets when no foot is in contact; the playground feeds tilt/height to the supervisor and installs the learned righter; the supervisor resets the righter on every FALLEN entry; the viewer loop resyncs instead of fast-forwarding after a stall; `RecoverEnv` reward `v3` (v2 + a smoothness cost on the servo target) and a per-checkpoint `--rate-limit` that `PolicyRighter`/`eval_recover` read back. Two v3 retrains (`recover5_v3` scratch 3 M, `recover5_v3_warm` from recover1 +3 M, both capped sigma, 3 rad/s) are recorded NEGATIVE on the handoff (2/20 and 4/20 vs recover1's 7/20 on the same seeds) — the warm one is the smoothest righter so far (58 % of tick moves pinned, 6.5 reversals/s, 2.1°/tick vs 73 %, 9.8/s, 4.4°), so the trade is real but not yet won; recover1 stays shipped. **The supervisor gains a STALL rule: no 10° improvement of the best tilt for 3 s → ramp now** (`stall_s`, `stall_tilt_deg`; `right_reason` records handoff/stall/deadline), because under the rim shove every landing is on the BACK and there the planted ramp rights the robot 5/5 while no checkpoint ever meets the handoff criterion: the demo's righting time drops from the 10 s deadline to ~5–6 s and the flailing with it. | The user's report ("the push and reflex correction flies and jitters") was right on both counts and neither was a physics bug. Flying: every push was a rectangular pulse at the CoM — the README demo's 120 N × 0.25 s was a 30 N·s strike (4.6 BW·s) that launched the 2.67 kg robot at 9–11 m/s and 11–15 m across the floor (the camera tracks the torso, so the floor streaks by); the model also has no gentle regime — below the ~31 N foot-friction limit the robot is a rigid block, above it the feet let go and it cartwheels. Under the rim shove the standing robot survives 25 N peak and tips at 30 N (~1 BW peak, 6–8 N·s), walking 20–30 N by direction; a 45 N demo shove felled only 3/5 seeds (two rolled through and landed upright), so 40 N was chosen. Jitter, source 1: `recover1` runs 73 % of its per-tick joint moves pinned at the 5 rad/s rate limit and reverses direction 4–23×/s per joint — a 50 Hz staircase of 5.7° jumps (`audit_righter.py`), i.e. the D045 bang-bang failure in the deployed policy; source 2: BRACE's tilt-vector seek flips its goal every physics step while airborne (58 reversals in 0.4 s, legs thrash mid-cartwheel), and the playground never told the supervisor the body had tipped, so it lay on its back cycling BRACE→RECOVER→NORMAL in a standing pose. Ruled out: servo stiffness (0.3° tracking standing, <7° walking), control-target smoothness on the ground (0 reversals in BRACE), loop pacing (5–9× real time headless). Also found: with the rim shove `recover1` never righted by policy in the demo (0/5; every stand came from the 10 s deadline ramp), which the old strike's random landings had hidden. |
| D049 | 09-23 | **The cockpit: a browser playground on ONE running sim (`sim/cockpit.py` + `cockpit_ui.html`, `./rocky.sh cockpit`).** A sim thread owns physics (the Playground loop with the harness goto controller + cliff detector folded in, real-time paced, speed/pause), renders a chase camera and a torso **eye** camera to MJPEG, and everything else — the HTTP API, the chat brains, the MCP proxy — reaches it through a job queue. Brains switch at runtime: Talk (regex intent), Local model (any llama-swap model with tool calling), Claude (API if keyed; else `./rocky.sh chat` drives the same sim because `harness/server.py` picks a running cockpit with `ROCKY_BACKEND=auto`). `look` is a real tool now: the eye frame to a local vision model. `sim/world_builder.py` composes worlds from a spec at runtime — presets, boxes/walls/ramps/stairs/rubble/a pushable ball, floor friction, a gravity tilt, a rough heightfield — and the sim swaps models in place. RL panel: checkpoint table, righter hot-swap, stall/deadline sliders, curves. | The MuJoCo viewer window can host neither a UI nor a second camera, and its own key bindings fought the teleop (D048); a browser can do all of it and needs nothing installed (starlette/uvicorn ship with `mcp`; MJPEG + SSE, no websockets). One sim for every driver was the design rule: before this, `rocky.sh chat`, `brain` and the playground each ran their own physics. The vision tool is honest about what it is: a vision model's words about a rendered frame, not a sensor. Verified end to end on 2026-09-23: talk chat (hello → chord + wave; forward 20 cm → goto arrived), local brain (qwen3.6-35b-a3b called `say` then `look`; `vision-model` described "a small obstacle to the left, open floor ahead" on the obstacle course), world swaps (rough terrain, stairs, added box, friction 0.5 + 5° slope), righter hot-swap, speed/pause, a 40 N shove → FALLEN → recovered → NORMAL in the cockpit, reset, and the MCP proxy's status/say against the live cockpit. |
| D050 | 09-23 | **Contact-seeking stance: a planted foot without contact feels for the floor (down to 30 mm at 120 mm/s, `Playground._probe`), and a void is a foot that ran out of probe (`CliffDetector.update(..., probed_out=)`).** With it the cockpit's goto crosses the rough-terrain heightfield, the rubble field and 4 × 15 mm stairs and still stops at the cliff-world edge; a 6 cm box ends the goto as `stuck` (no progress for 6 s; 40 s = `timeout`) instead of a false `cliff`. Same round: recordings (chase frames + every command → mp4/gif/run.json, replay on the live sim), saved worlds (`sim/worlds/*.json`) and a seeded random course, the PPO residual walker as a switchable gait (`Playground.residual`, applied while NORMAL), voice through ffmpeg → whisper-server, a quit endpoint, `rocky.sh cockpit-stop`, a phone layout. The harness's in-process sim backend and the `run_cliff` experiments keep the timing-only detector. | Two wrong turns first, both caught by re-testing the cliff preset after each change: "a void is a foot that reached BELOW the ground" can never fire with stiff position servos (the foot stops at its commanded z over nothing — the detector's own docstring says so) and the cliff world walked off its edge; "a foot held ABOVE ground is an obstacle" kept the cliff but still fired on rough ground, because there the feet are at ground level and simply bridged. Probing is the answer to both because it turns the missing information (is there floor a little lower?) into a measurement, which is also exactly D017's Phase-3 advice and what the bench robot can do with its microswitches. A separate bug found on the way: `set_world` compared the new name with itself and always respawned "in place", so switching presets from a far pose rebuilt the world around a robot standing inside a wall. |
| D051 | 09-24 | **The cockpit becomes the bench-side tool: gestures as keyframe data (`gait/pebble_keyframes.py`, `gait/gestures/*.json`, a studio panel), chord words designed in the browser (`audio/custom/*.json` + `audio/samples_custom/*.wav`, canon words read-only), a servo realism model (`sim/servo_model.py`: bus-rate hold, latency, slew, count quantisation — off by default), and a hardware bridge (`sim/hw_bridge.py` over `rocky_driver`) that mirrors the sim to whichever legs answer the bus scan, sim→robot or robot→sim, with jog, torque/limp, the SafetyMonitor, center/dir calibration into `bench/calibration.yaml`, ID assignment; `rocky.sh tailnet` publishes the cockpit over HTTPS with `tailscale serve`; chord audio plays in the browser.** | The robot is coming up one leg at a time and the question was what makes the sim useful for that. A leg on the bench needs (1) a way to send it exactly what the sim leg does and see the difference (mirror, per-present-leg, everything else stays simulated), (2) the same calibration model as the bench scripts so nothing is learned twice, (3) a sim that stops flattering the servo (the slew/latency model), and (4) content — gestures and words — as files that the bus will play back, not Python. HTTPS on the tailnet is not cosmetic: the mic API is refused on an insecure origin. Verified 2026-09-24 on the mock bus and in a real browser (no console errors): scan 20 → 5 legs, sim→robot streamed the stance (mock hip at 28.7°), a jog drops the stream, robot→sim followed, center wrote the offset, limp cleared torque; a two-frame gesture played and saved and appeared in every list; `yes` loaded into the designer, rendered (31.8 kB wav) and saved as a new word; the servo model on at 30 ms latency. **Not verified: any real servo** — the bus has never had one on this laptop. Found on the way: `rocky.sh` had five uncommented help lines since f830ed4 and failed to parse; fixed. And the 9c0416f SIGTERM fix never worked: uvicorn installs its own SIGINT/SIGTERM handlers when it starts (`Server.capture_signals`) and silently replaces one set before `uvicorn.run()`, so the immediate exit has to be the server's `handle_exit` itself; verified with a stream open. |
| D052 | 09-24 | **The sim stops flattering the servo; the studio cannot author what the robot cannot do; the bus gets a safe first move.** (1) **One servo identity**: `cad/params.yaml` gains `actuators` / `joints` (positions + speed budgets loaded 3.0 / free 4.0 / hard 4.7 rad/s) / `gait` / `reflex` / `sensing` (old keys are YAML aliases, not copies), read by one loader, `gait/rocky_model.py` (no numpy/mujoco, so the bus driver imports it). The MJCF and URDF generators, `servo_model`, `shove`, the driver clamp, the playground and the cockpit all read it; no hand-typed servo number is left in those. (2) **Physics**: joint damping = stall / no-load = 0.626 N·m·s/rad, leg forcerange = 0.65 × stall = 1.911 N·m (continuous, not stall — **superseded the same day by D052a**: stall 2.94 N·m peak + a thermal model), foot μ 0.8 (was 1.2) with torsional friction, the foot sphere's surface is the IK foot point (`foot_tip{i}` site), the 40 g claw double-count removed (2.670 kg), spawn 1.4 mm above settle instead of +14; world geoms get `priority="1"` so world friction (the icy preset) actually reaches the feet; a robot-only fingerprint (`sim/model_fingerprint.py`, `5a32f772ca99`). (3) **Gait + envelope**: T 1.6 → 2.0 s, step 32 → 24 mm, duty stays 0.8; `WaveGait.budget()` scales every command uniformly into the envelope (45.5 mm/s at any heading, 0.246 rad/s turning in place) and walk, teleop, goto, the residual walker, `gait_node` and `run_sim` go through it. (4) **One feasibility checker** (`gait/pebble_feasibility.py`: guarded limits, speed per class, jumps at entry/exit/phase edges, ≥ 3 feet, CoM margin from the MJCF's own segment masses — matches MuJoCo to 0.0002 mm — slip, self-contact) gates code gestures, keyframe files (save refuses a FAIL unless forced), the studio, gait presets and the brain's `compose_gesture`; a whole-body reach solver and teach-by-demonstration feed the studio. (5) **Always-on loop** in the Playground (so the cockpit inherits it): gestures blend in and out with the reflex still watching; one void guard for every command source (latched world bearing, refusal until `clear`); 3 gyro trips in 5 s latch a safe-stop; a lead-limited contact probe; kinematic height from the IMU + encoders; servo model ON; every target stream NaN-held and rate-clamped at 4.7 rad/s before the sim **and** the bus. (6) **RL trains on the robot's own path and sensors** (obs v2: EMA-filtered action, the servo model, IMU / 1-count encoder / foot-switch noise, no torso height); checkpoints carry a contract with the fingerprint and old ones replay flagged `legacy obs`; the handoff is `handoff_ok` (tilt + ≥ 3 switches + joint-angle height), computable on the Pi. (7) **The D051 hardware bridge made safe before any servo touched it**: sim → robot only from a planted standstill; each leg's goal parked where it is, 40 % torque, 200 c/s, a smoothstep blend, then release; fault bit or ≥ 65 °C limps the whole leg; silence → degraded / lost, re-armed only by naming the ids; NaN hold and 4.7 rad/s cap at the bus; 2 s heartbeat; port loss → limp + reopen; EEPROM angle limits 2° outside the soft limits (`bench/apply_limits.py`); the mirror is dropped the moment the sim's reflex leaves NORMAL or the sim respawns; locomotion held while mirroring; the mock servo can no longer move faster than 4.7 rad/s. `rocky_driver.soft_enable` / `SoftStream` give the bench scripts and the ROS node the same first move. (8) **Cockpit**: a sim-thread exception limps the legs, 503s every waiting request and exits 1 (before: every request hung forever); a Host allow-list + same-origin + JSON-only request guard, non-loopback bind only with `--unsafe-lan`; the brains move to `sim/cockpit_brains.py` (roles, fallback chains, quarantined models unselectable, a wake-word gate on voice motion); goto gets a reactive lidar layer (`blocked` after two detours). (9) **The same day: three follow-ups, then a review (D052a).** **P1 — torque is peak, sustained is thermal** (the owner's ruling): the 1.911 N·m clip was the wrong place for the continuous budget. A position servo's instantaneous torque is its stall torque; clipping every physics step at 0.65 × stall capped an unloaded joint at 1.911 / 0.626 = 3.05 rad/s (measured 3.06), below the servo's 4.7 no-load and below the 4.0 free budget the checker allows (this row's strict xfail). The clip is now stall 2.94 N·m (claws 0.23), damping unchanged, and 0.65 × stall is a heat budget enforced over time — D052a has the model and the numbers. **P2 — the shallow-angle void miss.** The wave gait (duty 0.8, five legs) lifts the next leg at the instant the previous one lands. At 10–20° the two leading legs (3 at −54°, 4 at +18° on the body) straddle the edge: leg 3 lands 40–50 mm past it as leg 4 lifts, and legs 0–2 cannot hold the CoM (for any two neighbouring legs of this pentapod the other three leave it outside their triangle), so the robot tipped in ~0.3 s, before the probe's 0.2 s settle and 30 mm descent had finished. The old retreat (walk backwards along the void bearing) made it worse: it lifted the next leg in the forward order while the void leg hung over the edge. Fix, in `sim/playground.py`: a **touchdown gate** — a stance foot whose switch has not confirmed ground 140 ms (GATE_TICKS 7) after its commanded touchdown runs the gait clock back to that touchdown and holds it while the foot probes (the leg that had just lifted goes back down); ground releases the hold, a probe-out fires the void, and the void detector counts a foot as settled by its probe's own tick count so it still fires during a hold. The **retreat plays the approach backwards** (the gait clock reversed for 0.6 cycles: every foot returns to a foothold it already stood on, and the void leg lifts first). `set gate.wait 1` is the **careful walk** (every touchdown waits for its switch), off by default. 7 ticks because the first switch contact lands 2–4 ticks after the commanded touchdown on flat ground and rubble; any threshold ≤ 5 held the gait on almost every flat step (29–31 holds in 15 s). **P3 — a spawn-pose bug older than D052**: `run_push`, `run_push_reflex` (so `run_push_reflex_v2`, `audit_righter`, `run_showcase`), `run_terrain`, `run_sim_manip`, and then `run_sim` and `test_model_consistency`, wrote the 15 leg angles into `qpos[7:22]`, but each leg's claw joint sits between its leg joints in qpos, so every leg spawned in the wrong pose and the torso tilted 9.8° in the first 0.13 s. Every push / terrain `tilt_max` therefore read 9.83° whatever the push, and the reflex calibration's startup gyro peak was 2.69 rad/s instead of 0.42. Fixed by joint address, plus `spawn_z_m` everywhere (drop at spawn 12.6 → 2.2 mm; `run_cliff`, `run_gestures`, `run_manip_adjacent` and `sim_lidar` still spawned at h + 14). Also: `ppo_smoke` takes obs_dim from the env (the hard-coded 41 crashed on 56); goto's `blocked` outcome is documented; the cockpit offers sim → robot only while the sim stands still at 1×, unpaused; a world change resets pose and yaw only when the world really changes. | The owner's measurement that morning: with damping = stall / no-load the gait still walks, but the ideal sim had been asking joints for 7.4 rad/s (the servo does 4.7 unloaded) and turning 157° where the damped model turns 145°, 103° with the torque derate — "the turn envelope must shrink and that is the correct answer". A review (4 reviewers, 29 verified findings) then showed the flattery was not one number: nothing checked what a gesture or command asked of a joint (fist_bump peaked at 14.8 rad/s, beckon 5.9, turn_in_place 5.2, the gait at 45 mm/s asked the knee 4.6 under load); the righter trained and was scored on an ideal actuator and a world-height handoff; the bridge would enable torque toward whatever goal a servo last held. **Measured on the D052 tree**: `run_sim` 246 mm of ~261 commanded (was 264), tilt max 0.84°, height 117 mm (target 118, was 129 — the stance is now the gait's h, not h + 13); envelope runs (6 s): walk 231 mm (ideal 261), turn in place 103° (owner: 103), walk + turn 76° yaw (owner: 74, ideal 103), peak joint speed 3.1–3.2 rad/s = the loaded budget (was 7.19 / 8.13). Gait physics under the budget: 239 of 256 mm forward, 72.8 of 80.3° turning. Gestures after rework: fist_bump 2.41, beckon 3.45, turn_in_place 3.63, jazz_hands 2.79 rad/s; `audit_gestures` 20 rows, 0 FAIL, nothing falls. Duty 0.75 (the obvious speed-up) was rejected by the checker: two adjacent legs swing together and the CoM sits 59 mm outside the planted feet for 25 % of the cycle. **What got worse, and why that is right**: the speed envelope exists at all (before, `walk 0 0 0.5` drove the yaw joint at 7.38 rad/s past its limit); mixed commands shrink (45, 0, 0.35) → (19.6, 0, 0.152); every walker on disk has **no** envelope on the D052 servo (the old gait's swing alone asks the knee 4.43 rad/s under load), so the cockpit zeroes them until retrained. The righters (re-measured on the peak model in D052a: 5/20 legacy vs 3/20 with the 1.911 clip, noise; 0/20 `handoff_ok`; system unchanged): `recover1` stands **2/20** on the legacy handoff and **0/20** on `handoff_ok` (0/20 with the nominal servo, 0/20 randomised, pure-RL 0/20); `recover5_v3_warm` 3/20 legacy, 0/20 hw, pure-RL 4/20 → 0/20. The old 7/20 was flattered twice: an ideal actuator, and a height test that passed a robot kneeling on its shins with 0–2 feet down. At the **system** level (`eval_recover --supervisor`: supervisor + righter + the 3 s stall ramp, on `handoff_ok`) it stands **20/20 vs 11/20 with no righter** (back 6/6 vs 0/6), but all 9 declared falls end on the stall ramp, none on a handoff, and both checkpoints give line-for-line identical output — the policy leaves a pose the ramp can stand; it never stands the robot itself. With the servo in the loop the jitter drops (recover1: 52 % pinned, 6.7 reversals/s, 3.3°/tick vs 73–75 %, ~10/s, 4.5°). The robot got *harder* to tip: standing survives 30–35 N (1.15–1.34 BW) where D048 said 25 N (**corrected in D052a**: an artefact of the 1.911 clip and the 5 N grid; on the peak model the 1 N-grid standing floor is 29 N, 1.11 BW), walking mean 0.91 → 1.15 BW (min unchanged, 0.76) — the slower, lower gait and the joint damping, not isolated; this may flatter in the other direction and needs a check. Tracking lag p95 is 13–19° on arm moves and 16.6–18.7° on every gait row (warn at 10, fail at 20): the sim is now close to saying the gait asks more than the servo follows. Found on the way: the icy-floor preset never changed anything (MuJoCo takes the max of the pair, and the feet said 1.2); 96/200 random courses put an object within 0.3 m of the spawn (now 0/200); `rocky.sh train-walk` passed an `--env` the trainer rejected; every MCP `gesture` to a running cockpit raised a TypeError (a `name` kwarg collision); the hands were checked against the 12 V window and raised a false alarm every poll; the first soft entry jumped 20° on a knee resting outside its soft range. Bridge rules are mutation-tested: 8 of 8 deliberate breakages caught. Fast suite 297 passed, 1 strict xfail. **Not verified**: **no real servo** — every bridge rule ran on the byte-faithful mock (GOAL_SPEED 0 = max, torque-enable heading to the last goal, behaviour at an EEPROM angle limit, the POSITION_OFFSET frame and sign are VERIFY-ON-BENCH); **the tailnet path is unexercised** (tailscale was logged out; the Host/Origin rules pass it only in tests); μ 0.8 and torsional 0.005 m, continuous_frac 0.65, every SCS0009 number, the foot-switch 2.0 / 1.0 N, the probe constants and all obs-noise levels are guesses marked VERIFY; no righter or walker trained on the new contract (two 2 000-step smokes only, B34); no real LLM tool loop and no Claude brain call ran; the new CI workflow has not run on GitHub. **Open**: the free budget (4.0 rad/s) exceeds what the MJCF lets any joint reach (forcerange / damping = 3.05) — a strict xfail until the owner picks peak torque + a thermal model or free ≤ 3.0 (closed by D052a); the void guard misses cliffs approached at 10–20° (the touchdown gate, item 9: walk 45 stop margins at the 350 mm edge HEAD → gate 0° 126 → 184 mm, 10° fell → 184, 15° fell (the guard never fired) → 115, 20° fell (fired late, on leg 1) → 121, 30° 131 → 139, 45° 170 → 177, settled 1.95–1.97 s after the first probe-out at every angle; 72 approaches (8 commands × 9 headings) 28 falls → 6, careful walk 1 but 17 % slower on every surface (flat 0.625 → 0.521 m in 15 s); flat-ground speed unchanged, rough terrain / stairs −2.6…−4 % from occasional holds, no false void on any preset — **still open** in a lip band: D052a); ~~the void retreat barely retreats~~ (fixed by item 9: it now backs off 39–59 mm); with no righter the supervisor on its back loops FALLEN → RIGHTED → FALLEN every 3 s. |
| D052a | 09-24 | **D052 amendment (owner) + its review: the MJCF clips at the servo's PEAK torque and the continuous budget becomes a thermal model; the heat is the motor CURRENT, judged as RMS; the live sim accounts it too.** (1) **Peak clip**: leg forcerange = stall 2.94 N·m (was 0.65 × stall = 1.911), URDF effort likewise; an unloaded joint reaches 4.71 rad/s (forcerange / damping; 3.05 with the motor held to the continuous torque), so the free budget (4.0) is reachable and D052's strict xfail is now a passing test. Claws: forcerange = SCS0009 stall 0.23 N·m and — review — damping = its own DC line 0.23 / 9.5 = 0.0242 (was a literal 0.01, i.e. ~23 rad/s unloaded vs 9.5 no-load); `check_urdf_parity` now also asserts forcerange / damping ≈ no-load for all 20 actuated joints (worst 0.004 rad/s). Robot fingerprint `ceb63a1254c3` (`5a32f772ca99` = the 1.911 clip; `4debe4e83893` = peak clip with the old claw damping, never used for training). (2) **Thermal model** (`rl_common.ThermalProxy`, constants from `params.yaml`): heat += ((\|τ_e\| / stall)² − 0.65²) dt, clamped at 0, budget (0.85² − 0.65²) × 180 s = 54 — 3 min at 0.85 × stall trips, 93.5 s at stall, never at ≤ 0.65, within ~10 % of the driver mock's 70 °C cut. RL envs: past the budget the joint's forcerange ramps to 0.65 × peak and the reward pays −0.5 × mean derate. **Review fix: τ_e = actuator_force − dof_damping × qvel** (`rl_common.motor_torque`), the current term — actuator_force is the motor's voltage term and the damping is its back-EMF, so the raw force counted a free joint spinning at no-load as stall-level heat (a free yaw swung 0.6 rad at 2 Hz, 4.70 rad/s peak: RMS force 0.87 × stall, RMS current 0.22; the raw-force proxy gained 19.3 of 54 per minute, the current proxy 0) and understated a back-driven joint; the 45 mm/s gait's load was overstated ~2× (mean \|F\| 0.184 vs \|F − Dv\| 0.094). (3) **`THERMAL_LOAD` / `THERMAL_LOAD_WARN`** in `pebble_feasibility.judge_load`, fed by `audit_gestures`: **review fix — RMS τ_e / stall, not mean \|F\|** (heat is τ²: a 50 % duty of stall is mean 0.50 / RMS 0.71 and trips the proxy at 691 s; the mean passed it clean). > 0.65 warns, > 0.85 fails; the column and JSON field are `load_rms`; gait rows now print their warnings. (4) **Live sim** (review): `Playground` runs the same proxy in accounting mode (no derate) from the electrical torque — `guard_status()['thermal']` (heat_max, hot joint, tripped), a note at 50 % and at the trip, a cockpit `servo heat` chip past 50 %, and **sim2real is refused while any joint is past its budget**. The studio/`compose_gesture` gates stay kinematic (the thermal verdict lives in `audit_gestures`). (5) **Review fixes in the same round**: sim2real could start during a void retreat (idle_reason / is_idle checked only the ask, which the void guard zeroes, while the retreat kept walking) — `Playground.motion_reason()` (retreat, non-zero effective command, pending safe-stop, gate hold) now gates is_idle, idle_reason and the UI, and a retreat ends where it is if the mirror holds locomotion; the first edit of a preset world teleported the robot to the origin at yaw 0 (the edit renames the world `custom`, and same-world was decided by name) — `/api/world/add` passes `keep_pose=True`; a touchdown-gate hold is released when the command CHANGES, not only at zero; the gate's rewind runs at min(3, free / the gait's swing peak, loaded / its stance peak) — 1.12× at 45 mm/s — so on stairs / rough terrain the non-probing joints stay ≤ 3.94 rad/s (the 3× rewind drove 40–46 % of hold steps past 4.7 and relied on the clamp); a 310-approach cliff grid shows no change in falls from it. | **Measured on the amended model (2026-09-24)**: `run_sim` 246 mm of ~261, tilt max 0.84°, height 117.0 mm, turn in place 55.4° (budgeted 0.246 rad/s, ~54 commanded); `audit_gestures` 20 rows, 0 FAIL, worst RMS load 0.39 × stall (`manip_adjacent`), gait rows 0.10–0.12, every other gesture ≤ 0.14; turn-in-place gait row 73.0° of 80.3° (6 s). **Shove envelope re-measured** (`sim/shove_envelope.py`, 5 N grid): standing 25/35/35/25/30/30 N by direction, min 0.95 BW / mean 1.15 (the 1.911 clip gave 30/35/35/30/30/30, min 1.15 / mean 1.21), walking min 0.76 / mean 1.08 (was 1.15). **The 5 N grid rounds a 2 N shift**: on a 1 N grid the standing tip threshold is **29 N (1.11 BW)** at 0° and 180° vs 31 N with the 1.911 clip (240°: 31 vs 34 N; 60°: ≥ 37 both; walking at 120°: 24 vs 25 N). So: standing 29–≥ 37 N (floor 1.11 BW), walking 20–35 N (min 0.76 BW), and the 54 % more torque makes Pebble slightly *easier* to tip, not harder; D052's standing 30–35 N was the 1.911 clip plus the 5 N grid. One mechanism probe (29 N at 0°): the 1.911 model saturates 6 joints (6.4 % of samples) and slides 106 mm — yielding legs turn the shove into sliding — while the 2.94 model saturates 2 (knees to 2.38 N·m), slides 78 mm and pivots over the downhill feet; at 30 N it tips (68°) and the 1.911 model survives. One probe, not a proven mechanism. **Re-measured on the peak model, with an A/B** (the same tree with the leg forcerange put back to 1.911 N·m in memory; it reproduces D052's own shove and env numbers exactly): in normal operation nothing moves, because no joint reaches the old clip — `run_sim`, `rocky_env.sanity` (zero action, 45 mm/s, 400 steps: servo nominal / off / random 331 / 334 / 293 mm vs 330 / 333 / 293), `audit_gestures` (same peaks, load within 0.1 pt, p95 within 0.5°) and the cliff stop all match the 1.911 control within 1 mm and 0.3°. **`recover1`**: `handoff_ok` **0/20** (back 0/6, side 0/7, tumble 0/7; end tilt median 16.6°, best 3.5°; hold-pose 1/20, random 0/20), pure-RL 0/20; legacy handoff **5/20** (side 3/7, tumble 2/7) vs 3/20 with the 1.911 clip on the same tree (D052: 2/20) — inside 20-episode noise, not an improvement; system **20/20 vs 11/20** (back 6/6 vs 0/6, side 7/7 vs 5/7, tumble 7/7 vs 6/7), 9 declared falls, exits 0 handoff / 9 stall / 0 deadline, t_stood median 0.46 s vs 0.80 with the 1.911 clip — the stall ramp stands the righter's pose faster, the counts do not change. **Cliff** (yawed spawn, 1 s settle): walk 45 at 0/10/20/30/45° — void at x 164/166/219/202/166 mm, max x 166–230 (edge 350: 120–184 mm to spare), backs off 39–52 mm, torso ≥ 270 mm, 0 falls; walk 25 — max x 184–219, backs off 14–28 mm, 0 falls. D052's single walk-45 0° run fired at x 212 and backed off ~25 mm; the A/B puts that difference on the gate / retreat edits, not on the torque. **Still only on `5a32…`**: the 6 s envelope runs (walk 231 mm, turn 103°, walk + turn 76°), `recover1` with the nominal / randomised servo and its jitter, `recover5_v3_warm`, and the RL smokes. The tracked `sim/push_results.json`, `push_reflex_results.json`, `terrain_results.json` (+ `_ext`, v2, figures) and `run_push_reflex_v2.py`'s TRIP = 1.8 calibration predate D052's spawn-pose fix (`qpos[7:22]` sliced a claw, a 9.8° spawn tilt) and this amendment — they are historical until regenerated. **Void guard — correction**: D052 P2 (the touchdown gate) was reported to stop at every approach angle; the review found the grid stepped over a band. A 1° grid (walk 15/25/35/45 × −30…60°, 310 approaches, 14–30 s each): **late gate 7 falls** (45 @ 13, 14 and 12.5 / 13.5 / 14.5; 25 @ 15–16; 35 @ 20–21; 15 @ 17.5), **careful walk 9 falls at other angles** (14–20°). Mechanism: a leading foot lands on the lip (sphere centre 0–8 mm past the edge) and gives way while the next leg swings, or the next leg lifts as the other leading foot lands in the void; the gate reacts 140 ms after touchdown and the rewind replays the swing arc. Tried and rejected: re-seeking a planted foot whose switch opens while another leg swings (fixes the band but the flicker at every handoff and on stairs / rough ground / ice ratcheted the probes to false voids — 18–27 holds and a void on flat ground), the same gated on tilt rise ≥ 1.5° + a tilt-confirmed void during holds (terrain clean, but 7 → 7 falls late, 9 → 8 careful), GATE_TICKS 5 / 6. Pinned as `test_void_guard_lip_band_known_gap` (strict xfail, 2 fast + 3 slow cases). Fast suite 325 passed, 2 strict xfail (5 slow deselected). **Not verified**: the thermal constants 0.65 / 0.85 / 180 s (a 10-minute bench hold with a temperature log decides them); from cold the proxy cannot trip inside an 8 s gait or 6 s recover episode, so training only meets a derated joint through `thermal_heat0` warm starts (an env argument, off by default, no trainer flag yet); between 0.65 and ~0.77 × stall the proxy trips slowly (800 s at 0.70) where the driver mock never does — conservative there; the claw's DC line is as much a guess as every SCS0009 number; the lip band is open; making the careful walk the default (6 → 1 of 72 falls, 17 % slower everywhere) is the owner's call. |
| D053 | 09-24 | **The robot's shape is data (step 1 of `docs/ROBOT_AS_DATA.md`); zero behaviour change.** `cad/params.yaml` gains a `robot:` block (legs: count 5, first station 90, radius; the per-leg chain yaw / hip / knee with axes and offsets, the foot, the claw as a tool, the IK solver) whose every number is a YAML alias of an existing key (`&l1_coxa`, `&hip_axis_z`, `&l2_femur`, `&l3_tibia`, `&leg_servo`, `&claw_servo`, `&body_r`; values unchanged, `params_rev` stays D052). `gait/rocky_model.py` builds a frozen `RobotSpec` from it (`robot()`, lazy and cached; absent block = the same spec from leg / body / bus) and `validate()` refuses what the code cannot honour yet: id rows vs leg count, a row vs the chain, duplicate ids, a joint without a range, an unknown servo, analytic IK on a chain that is not yaw + 2 parallel pitches, a redundant leg without a redundancy rule, uneven stations, non-empty `overrides`. `n_legs()`, `stations_deg()`, `actuator_order()` (the ctrl[:15] / ctrl[15:20] contract as a named fact), `id_table()`, `topology_hash()` (`7f066d9bd8c0`) and `description_digest()` are how code reads it; `LEG_JOINTS` / `ALL_JOINTS` come from the spec via PEP 562, so `import rocky_model` stays pure and never validates. `pebble_gait` N_LEGS / STATION_DEG, `build_mjcf` (stations, leg count, actuators in `actuator_order()`) and `generate_urdf` (stations, leg count) read it. | `sim/pebble.xml` and the URDF are byte-identical; robot fingerprint still `ceb63a1254c3`; `check_urdf_parity` OK with 20 joints. New `gait/test_robot_spec.py` (24 tests: the spec equals the pre-D053 literals, a generic chain FK equals `leg_fk` to 1e-9 mm, `actuator_order()` equals the compiled ctrl order, eight refusals, a 6-leg and an ankle fixture that validate with 24 / 25 actuators and new topology hashes, purity + a broken block that leaves `actuator()` working) and three model-consistency tests (compiled links = spec, URDF xacro not stale, URDF joints = spec). Fast suite 454 passed, 2 strict xfail. Measured in scratch: a 6-leg params already generates a compiling 24-actuator MJCF and a (6, 3) WaveGait, but `rl_common.N_LEGS` is a literal 5 and `mass_audit` counts 5 legs; an ankle is refused by MuJoCo (the leg template has 3 bodies) and silently ignored by the analytic gait IK — steps 2-10 (B36) close these, in order. `docs/DESIGN_CHANGE_GUIDE.md` is how to change servo, lengths, foot, material or topology today. |
| D054 | 09-24 | **Talk to it: the voice path goes from 20 s to 1 s, the robot gets an eye that measures, a memory, and an all-in-one brain.** (1) **Speech**: `whisper-fast.service` (:8086, base.en on the CPU, 8 threads, greedy) answers a 3 s command in ~1 s where the shared large-v3-turbo on 6 CPU threads took 18–20 s; `rocky.sh cockpit` prefers it; a vocabulary prompt and `language=en` go with every request; the mic button is hold OR tap-to-toggle with pointer capture and a timer, a lost recording says how short it was, and **hands-free** keeps the mic open with a level meter and sends each utterance as trusted; the wake word is fuzzy (Pebbel/Peble pass, people fails). (2) **Brains**: `lfm2.5-vl` (3B) does the robot's tool calls in 0.2–0.5 s warm; a verified research round (`docs/BRAIN_MODELS_2026-09-24.md`, 22 candidates checked against Hugging Face and the pinned llama.cpp) picked **Qwen3.5-9B** (+ mmproj) as the all-in-one: measured 7.2 GB VRAM, 6.5 s cold, 57.7 t/s, tool calls 0.2–1.5 s, `look` works; it is the default multimodal role and runs alone (never in the `resident` group); the system prompt now spells out that distances are metres after it answered "thirty centimeters" with x=30. (3) **Vision that measures**: `find_object` asks the vision model for a BOX and turns the box's floor point into bearing and distance through the eye camera's height and tilt (`pixel_to_floor`): median bearing error 0.7° (lfm2.5-vl) / 0.75° (gemma-4-26b-a4b), distance 3 cm, 3/3 finds each; asking the model for the numbers directly was 10–15° off because it copies the prompt's examples. A VLM is NOT a cliff sensor (1–3 of 4 right on "does the floor end ahead?", unstable between runs): the foot probe keeps the edges. (4) **Memory and awareness** (`sim/scene_memory.py`): pose-tagged observations and located objects per world, merged within 0.25 m, persistent, with `remember / where_is / recall / go_back_to / forget` for every brain and the MCP server; a situation line (pose, guards, lidar by direction, last look, memory, servo heat, bus) composed every 20 s while idle and prepended to the current user turn (not the system prompt: prompt-cache); chord reactions (alarm on a latch or bus cut, curiosity on a new obstacle within 0.5 m while standing still) at most one per 30 s. Measured: find ball → remembered 3.4 cm from truth; walked 0.3 m away; `go_back_to` arrived in 6.5 s at 0.37 m as designed. (5) **B34 first retrain** (`recover6_d052`, obs v2, 3 M, 25 min on the CPU): 0/20 hardware handoff, supervisor 11/20 = no-righter 11/20 — NEGATIVE, recover1 stays. (6) The phone-first front end (bottom tab bar, remote mode, follow-the-leader shared view, in-app guide, wake-word card) is in the same round; see the build log. | The owner was holding a phone that said "nothing heard" and then waited 20 s per sentence. The 20 s was the whisper service, not the network or the phone; the "nothing heard" was a lost long press the page could not explain. Each fix was measured before it was written down. The model research was made adversarial on purpose (every candidate verified by a second agent, nothing downloaded until picked) because the fleet has a history of models that fault; the pick was then measured, not trusted. The box-geometry finding is the useful surprise: a 3B vision model plus the known camera pose beats a 26B model asked to estimate, and the sim bench (`sim/vision_bench.py`) is repeatable. NOT verified: any of this on hardware (the eye camera is the sim's); the 9B's multi-hop conversations at length; GPU whisper (rejected for VRAM today, viable once the 9B is the only resident). |
| D055 | 09-25 | **Brains are installed and measured, not assumed; stop never waits for a model; relative moves are a tool.** (1) `sim/brain_install.py` (`rocky.sh brain-install`): a downloaded GGUF is read by its header, filed under `/mnt/models/<id>/`, paired with its projector (the folder, Downloads, a hard link from a sibling id, or `hf download`), added to the restore manifest and to llama-swap as an UNMEASURED stanza from the family template (never the resident group); a non-brain architecture (a FLUX image model) is skipped; copy-verify-fsync before the source goes. (2) `sim/brain_bench.py` (`rocky.sh brain-bench`): one model at a time on a clean GPU (llama-swap only, 20 s idle between spawns), cold first answer, VRAM delta, decode t/s from llama-server timings, 20 spoken lines scored against the tool schema ('thirty centimeters' must be 0.3, a refused stop is a miss, unasked motion is unsafe, a failed turn is still scored for safety), describe turns with the ball at known sides, the vision bench's detect and floor cases; its own cockpit (never :8765) with scratch copies of the config, the memory and the gesture library (`ROCKY_GESTURE_DIR`, after a model's `compose_gesture` saved `happy_dance.json` into the repo). (3) Measured, multimodal role, same prompt: `qwen3.5-9b` 17/20, 0 stops missed, first action 0.9 s, 6,997 MiB, 63 t/s → the default; `qwen3.5-4b` (Q8_0) 16/20, 0 missed, 0.9 s, 5,841 MiB, 72 t/s → the small brain; `qwen3.5-9b-q6k` 17/20 at 9,173 MiB and 43 t/s → no gain, delete; `gemma-4-12b` (UD-Q6_K_XL) 17/20 but a bare 'stop' answered with a chord, 11,325 MiB, 33 t/s, boxes 0.7° once read x-first; `gemma-4-26b-a4b` 15/20, both stops missed, 42 s cold → vision fallback only; `lfm2.5-vl` 5/20 with tool text in its replies → the eye only. **Box order is per model** (`BOX_Y_FIRST`): the 26B writes [ymin, xmin, ymax, xmax], the 12B unified-vision model writes x-first — both measured, a name rule had cost the 12B 21.9°. (4) **Stop gate**: every operator line in every model mode goes through intent's stop words before any model (minus "don't stop", "non-stop", "stop sign", "bus stop"), before the mode lock, without the wake word; a stop refuses the interrupted turn's later motion (`operator_stopped`) and any line queued behind it; `ROCKY_STOP_FIRST=0` exists only so the bench can measure a model's own stop handling. (5) **`move(forward_m, left_m)`** in the robot frame runs the goto path unchanged (guards, result vocabulary, the 1.5 m envelope) in the local-brain schema, the cockpit and the MCP; the prompt no longer asks a model for trigonometry (a 3B's 'forward 30 cm' had gone 96° off). (6) Prompt turn discipline: no second motion after the asked one has ended, one chord per turn (the 9B went 16→17/20, p95 latency 16→7 s, describe 3/6→5/6). (7) Cockpit: a world change or reset forgets the eye context, `/api/state` carries `event_seq`, the bench cockpit dies with its parent. Numbers and the verdict: `docs/BRAIN_MODELS_2026-09-24.md` §Status 2026-09-25. |
| D055a | 09-25 | **A per-family prompt note is worth more than a bigger model: explicit numbered rules (stop = stop only; ONE tool, called before any text; one sentence after; chords for greetings and feelings) lift Gemma 12B from 17/20 to 20/20 and Qwen3.5-9B from 17/20 to 19/20 on the brain bench, with no missed stop and lower latency; thinking ON drops the 12B to 16/20 at 13 s median.** `FAMILY_NOTES` in `sim/cockpit_brains.py` (prefix-matched on the model id, on by default for `gemma` and `qwen3.5`, `ROCKY_FAMILY_NOTES=0` off; `ROCKY_THINKING_MODELS` lists ids that get `enable_thinking`). The system message is rebuilt per model in the fallback chain, so a text fallback never inherits another family's note. Roles unchanged (the 9B stays default on latency and VRAM; Gemma 12B is the accuracy option). Rows in `docs/BRAIN_MODELS_2026-09-24.md` §Status. |
| D056 | 09-25 | **Tools are data: one registry, `harness/capabilities.py`, defines every tool a brain can call, and the local-brain schema, the cockpit's tool list, the MCP server's tool list and `docs/TOOLS.md` are generated from it; zero behaviour change.** (1) **Why**: the tools lived in four places that had drifted apart: `harness/local_brain.build_tools` with the `*_DOC` texts, `sim/cockpit_brains` (`EXTRA_TOOLS`, `GATED`, the executor `Brains.tool`), `harness/server.build_server` (FastMCP docstrings) and `harness/cockpit_backend` (the HTTP proxy). The registry survey recorded 17 disagreements: say, gesture, stop, scan_summary, status, list_gestures, look and find_object read differently on MCP than to the local brains; `GATED` was a tuple with duplicates, and talk mode's `intent.MOTION_TOOLS` lacks move and turn; the MCP server read the gesture and chord-word lists once at start; `turn` is callable at `/api/tool/turn` but offered to no model; compose / check / save_gesture exist only for the cockpit's brains. Adding a tool meant editing four files and hoping they agreed. (2) **The registry**: 19 `ToolSpec`s in today's order (18 offered to models, plus the executor-only `turn`), each with name, kind (voice, motion, query, meta, gesture_authoring, memory), OpenAI parameters whose gesture and chord-word enums are live-list markers (`$live`), the local-brain text and, where MCP's text differs today, `mcp_description` (both moved verbatim, FastMCP's docstring indentation included), MCP arguments and annotations, `gated`, `requires` (`eye`, `memory`, `cockpit`: a backend without it does not offer the tool), the backend method and the surfaces (openai, mcp, internal). `build(gestures, lexicon, signed, has_eye, has_memory, is_cockpit)` returns a JSON snapshot (format, version, capabilities, gestures, signed, lexicon, envelope, robot, tools) whose `version` is the first 12 hex of its sha256 (`c96ea6b3afbd` for the no-cockpit snapshot); `to_openai_tools`, `to_mcp_specs`, `gated_names` and `to_markdown` generate the surfaces (Claude mode converts the generated OpenAI list inline in `cockpit_brains`; `to_anthropic_tools` is the same conversion, tested, but nothing calls it yet). It imports only `harness.backend` and never prints (stdio MCP servers import it). (3) **Envelope and robot**: the snapshot's `envelope` comes from `WaveGait().max_command()` on `params.yaml` (0.0455 m/s, 0.246 rad/s, 24 mm step; goto 0.045 m/s and 1.5 m reach; `goto_max_m` is that advised reach, not goto's refusal, which stays 3 m in `cockpit_brains.GOTO_MAX_M`) and its `robot` from `rocky_model.robot()` (topology `7f066d9bd8c0`); the goto and move texts take their limit from the envelope (`docs/ROBOT_AS_DATA.md` §9). (4) **The MCP list is live**: built from the registry with the backend's current gestures, chord words and capabilities, not from lists read once at start. (5) **`docs/TOOLS.md`** is generated (`python -m harness.capabilities --md --out docs/TOOLS.md`), and a CI step after the D052 regeneration check runs `python -m harness.capabilities --check docs/TOOLS.md`, which fails on a stale page. | **Zero-change proof**: before any registry code existed, the surfaces were snapshotted: the OpenAI list from `build_tools` with every capability on, `local_brain.TOOLS`, `cockpit_brains.TOOLS`, the MCP `list_tools` of `build_server` on a full backend (15 tools) and on a plain `MockBackend` (8), and the sets (`GATED`, `TOOL_NAMES`, `MEMORY_TOOLS`, `NOTED`, `intent.MOTION_TOOLS`, the `CockpitSim.tool_*` methods, `SIGNED`). The registry reproduces them: the OpenAI lists byte-identical, key order included; MCP names, order, descriptions, input schemas and annotations equal for all 15 and all 8; `gated_names` equals `set(GATED)`; the registry's names equal the cockpit's dispatch set. `harness/test_capabilities.py` (21 tests) pins the sha256 of each snapshot (`GOLDEN`), so the proof still runs in CI without the snapshot files, and after the consumers read the registry, when a live comparison would be circular. `build()` alone cannot express `build_tools(look=False, extra=EXTRA_TOOLS)` (it keeps `find_object` without `look`, and the registry ties find_object to the eye); `build_tools` still reproduces it byte for byte because it appends `extra` as given, pinned with the other variants in `test_harness._BUILD_TOOLS_BEFORE`. **Adding a tool now**: one `ToolSpec` in `REGISTRY` (name, kind, parameters, the text; `mcp_args` and annotations if MCP should offer it; `gated=True` if it moves the robot; `requires` for what it needs), its handler where it runs (the cockpit executor behind `/api/tool/<name>`, and for MCP the backend method `backend_method` names), then `python -m harness.capabilities --md --out docs/TOOLS.md`. A new tool or a changed text changes the snapshot hashes: update `GOLDEN` (`harness/test_capabilities.py`), `_BUILD_TOOLS_BEFORE` (`harness/test_harness.py`) and `EXTRA_BEFORE_SHA` (`sim/tests/test_cockpit_brains.py`) in the same commit, as the record that the change was meant. |
| D057 | 09-25 | **Places are recognised from what the robot senses, not from the world's name: `sim/place_memory.py` compares a lidar signature, a look-description embedding and (on the real robot) a Wi-Fi fingerprint with every place it has stored, answers known / new / ambiguous with a confidence (never known from one sense alone), and declares a change only when two looks from different angles agree.** (1) **Fingerprints**: the scan as 72 numbers, the nearest return per 10° of map heading (compared at the best of 36 rotations, so a yaw offset costs nothing and the winning shift estimates it) plus the sorted range histogram (no yaw in it at all), log-scaled so a near wall counts more than a far one; the description as the cosine of two embeddings from llama-swap's always-warm CPU `embedding` model (~50 ms, no GPU), mapped 0.25 → 0 and 0.90 → 1 (re-set this round by replaying the first bench run's looks, the THRESHOLDS note in `sim/place_memory.py`; the run itself used 0.55 → 0); the radio as a weighted Jaccard of `{bssid: rssi}`. (2) **One score**: only the signals both sides have, weighted scan 0.5 (down to 0.2 of that for an empty sweep), description 0.35, radio 0.15, pulled toward 0.5 when the evidence is thin and capped at 0.45 of full evidence when only one sense was compared (`SINGLE_EVIDENCE`): a perfect lone sweep or description stops at 0.725, radio alone at 0.65, an empty sweep alone at 0.60, so no single sense can say known, and the answer names the senses it compared (`signals`; `[scan only]` on the situation line). known = best ≥ 0.75 and 0.08 ahead of a differently named runner-up; new = best < 0.5; ambiguous in between; unknown without a signal. `confidence` is always the best place's score (0.41 on new = the nearest known place is 0.41 alike), and `verdict_text` puts it on the situation line in at most 90 characters (`place: basement (0.91)`, `place: NEW (best basement 0.41)`, `place: basement? (0.62, or bedroom 0.58)`). (3) **Changes**: `checked_objects` turns the eye's per-name box answers (True = boxed now, False = asked and not there, None = no usable answer) into added / missing / present / unobservable, missing only where the stored spot is visible; `diff_objects` (positioned objects) calls a thing missing or moved only where a `visible(x, y)` predicate says the look could see its old spot, and missing only when the look's description does not name it (`mentioned`); otherwise nothing is claimed. `confirm_diff` keeps what two looks agree on (name, position within 0.3 m by default; the cockpit uses 0.5 m) and holds the rest as pending; a change is shown only on a known place. (4) **Storage**: `places.json` in the scene memory's pattern (atomic, debounced, a corrupt file moved aside, `.bak` before a wipe), up to 8 samples of each fingerprint per place; pure Python + numpy, the embedder injected, so it runs unchanged on the Pi; `parent` is reserved for home → room → spot. (5) **The cockpit** (`sim/cockpit.py`, off by default: `--recognize` or `POST /api/awareness {recognize: true}`; off, the memory is keyed by the world and the situation line, the system prompt and the models' tool list are the D056 ones, because the place tools need the registry's `places` capability; on, the look loads the vision role, so a world load or reset can move models on the GPU): after every world load or reset, once the robot stands still, one sweep + one look + its embedding → `recognize`; ambiguous and new take a second look after a 30° `turn` and combine the two; a place still new is stored as `new place #k` with both looks as samples (`enroll_samples`); known records a visit and runs the change check, which since this round asks the eye (for each remembered thing and each thing the description mentions, walls and doors left out: `Brains.detect(name, method="bbox")`, the `/api/look {find, method: bbox}` question, projected onto the floor; at most 6 questions per look; a "no" counts only where the stored spot is in view) instead of reading objects out of the prose, and any change takes the second look, which asks again, before `confirm_diff` declares or stores it. A failed look has no objects (never "nothing there") and leaves a scan-only verdict. A world edit (the pose kept) and talk mode's "this X has a new Y" (`place_check`) are change checks of the bound place only, never a new verdict or another place. `name_place` corrects a wrong recognition (a name that is another place's, or differs from the recognised place's own: what the visit wrote into the wrong place is taken back and moved to the right one), `rename=true` renames, `new=true` stores one new place per visit. The scene memory is keyed by the recognised place (`place-<id>.json`, a RAM-only provisional memory until a verdict), the situation line starts with the place clause, the state feed carries `place`. Four registry tools, `where_am_i`, `name_place`, `places`, `forget_place`, offered to the cockpit's models, and by the MCP server over a cockpit (`CockpitBackend` / `AutoBackend` forward them; the list follows the cockpit's `places` capability on the next `tools/list` or 3 s watcher tick), only while recognition is on; talk mode maps the owner's sentences onto them; `GET` / `POST /api/place`. (6) **The place bench** (`sim/place_bench.py`): rooms a-c enrolled, then same pose, a room loaded under the OTHER room's world name (`name_leak`), moved + turned, a never-seen room, + chair, - ball; opaque world names, a fenced own cockpit on :8795; its record `sim/out/place_bench.json` is committed like `sim/out/brain_bench.json`. Docs: `docs/SIM_GUIDE.md` §3b (Places), `docs/COCKPIT_GUIDE.md` (Where am I). | The scene memory is keyed by `memory_key(world_name, spec)`, and a world's name is ground truth a real robot never has. The owner's target is "hey, I'm in the basement at home" / "hey, this is completely new" / "hey, this master bedroom has a new chair", with two rules he agreed to: a single glance never declares a change (a second look from another angle first), and every verdict carries its confidence. **First place-bench run, 2026-09-25 19:39** (lfm2.5-vl + the CPU embedding model, 3 trials × 7 cases, 2.0× physics, `sim/out/place_bench.json`): **15/21 correct, 0 wrong "known"**: known → known 12, ambiguous 6; the never-seen room new 3/3 at 0.26–0.29; the name swap 5/6 with no name leak; 0 false alarms in the 15 unchanged visits, 0 changes declared from one glance; the second look taken in 14/21 visits; arrival → verdict 3.7 s median, 4.9 s p95. The true matches sat at the threshold: known 0.755–1.00 (median 0.86), ambiguous 0.67–0.735. Per sense, the scan was 1.00 at every same-pose revisit (0.87 with the chair, 0.79 moved + turned, 0.00 for the never-seen room), while the description (scores under the run's 0.55 → 0 map) scored the right room 0.26–1.00 (median 0.62) and the never-seen room 0.64–0.71 against its nearest stored one: every ambiguous verdict is a scan of 0.87–1.00 pulled under 0.75 by a description score of 0.26–0.52 (lfm2.5-vl mostly describes the floor of a 320 × 240 frame). **Changes 0/6**: in trials 2–3 both changed rooms came back ambiguous, so no change check ran; in trial 1 the check read the objects out of the look's prose (`objects_from_description` places a thing only when one sentence gives a direction AND a distance, and lfm2.5-vl said "a few body lengths"), so the chair was never placed and the ball had never been placed at enrolment to go missing. The two-look rule held the other way: in 8 of the 15 unchanged visits one look placed a phantom "wall" from the prose, the second did not agree, and it stayed pending. **What changed after the run, this round, not re-measured**: (a) the description map was re-set from its records (above): the right room's descriptions read cos 0.64 to 0.90+ (median 0.77), a wrong room's 0.60–0.89 (median 0.79), overlapping completely, so in these rooms the description does not tell rooms apart; the shallower map (0.25 → 0) only stops a re-worded look of the right room reading as a mismatch; replayed on the run's first looks, all 18 revisits come out known (weakest 0.779), the never-seen rooms ≤ 0.346, room b at its enrolment vs a 0.56–0.68 (ambiguous, never known); KNOWN_T 0.75 and MARGIN 0.08 kept (0.025 over the one-sense cap, 0.029 under the weakest true match); a replay of one run of three trials whose looks repeat, not a measurement; and it cannot break a lidar twin: a same-shaped room with bench-like descriptions (cos ~0.82) reads ~0.85, known; (b) the change check asks the eye with box questions; (c) a new place takes a second look before it is stored. The bench has not been re-run since. **Scan similarity** (synthetic ray-cast rooms, 360 rays, the `room` preset's 3.2 × 2.6 m walls and pillars): the same spot with 1 cm noise 0.9996, 0.1 m away 0.94–0.98, 0.3 m away median 0.71, 0.5 m away 0.24–0.63 (hence several samples per place), a room of another shape ≤ 0.005, the same walls with other furniture 0.84: same-shaped rooms are lidar twins, the description did not separate the bench's rooms (above), and the radio or the eye's per-object answers have to (neither measured). In MuJoCo at the spawn the scan plane is 0.179 m, and 8 of the 9 general presets give the lidar nothing or next to nothing (seven return nothing, `obstacle course` 37 of 360 rays from its one 0.25 m wall; `room` 360 of 360); the bench's rooms a and b are 0.49 alike to the lidar at the spawn (0.53 at the bench's enrolment). MuJoCo rooms are simple (flat-shaded walls and boxes on a checker floor, a perfect ray-cast lidar, a clean 320 × 240 render at 86° HFOV), so every sim number is an upper bound for the real camera and puck. `sim/tests/test_place_memory.py` pins the verdicts, the single-sense cap, the two rules and the 90-character cap; `recognize` over 50 places × 8 samples takes ~28 ms. **Assumed, not measured**: the weights, the verdict thresholds beyond that one replay, the 6-question budget per look, the cockpit's 0.5 m same-object distance, and every sim revisit, which starts from the exact spawn pose it enrolled from (a real robot comes back from anywhere, tilted, with a map frame that restarts every session; `scan_align` recovers only the yaw between visits, at 10°, and a rectangle can alias it by 180°). **The real robot adds** the radio fingerprint, its Wi-Fi scan, which the sim never produces: it tells buildings apart far better than the rooms of one building, so it weighs little and can never say known alone; and a real camera, whose descriptions will be noisier than clean MuJoCo renders. Open (B38): the home → room → spot hierarchy, the radio on the real robot, a recognition switch and the place on the cockpit page, the real camera (with a translation between session map frames). **Second run the same evening (21:15), after the fix round:** 21/21 verdicts, 0 wrong "known", true matches at 0.84–0.98, the never-seen room 0.33–0.37 (a lead of only 0.01–0.06 over its runner-up); changes 3/6 — every removed ball caught by the eye's box query on both looks, the added chair never (the eye calls it a box and denied it on the second look from the same spot); room b, which shares room a's walls, is ambiguous (0.65–0.69) until named. Thresholds: `DESC_COS_FLOOR` 0.25 set from the first run (pinned by a test that replays its rows), `KNOWN_T` 0.75 kept. |
