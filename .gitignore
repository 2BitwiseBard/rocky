# --- Python environments and caches ---
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/
.ipynb_checkpoints/

# --- bench outputs (calibration is per-robot, written on bench day) ---
bench/out/
bench/calibration.yaml

# --- local machine config (the .example is the portable one) ---
.mcp.json

# --- OS noise ---
.DS_Store
*.swp

# --- RL smoke / scratch runs (real runs are committed on purpose, git-lfs) ---
sim/runs/_*/
sim/runs/smoke_*/
# --- regenerated experiment clips (run_gestures2.py) ---
sim/pebble_gesture_*.mp4
sim/pebble_gestures2.mp4
sim/playground_clip.mp4

# playwright MCP scratch (screenshots, snapshots)
.playwright-mcp/
MUJOCO_LOG.TXT
