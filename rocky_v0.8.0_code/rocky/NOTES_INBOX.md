# Notes Inbox — dump anything here, Claude files it next session

*(measurements, print results, arrivals, ideas, questions — no structure needed)*

---

2026-09-01 — laptop sim env stood up (Claude Code session, Alienware/Pop!_OS). Venv at repo root `.venv` (uv, py3.12). Deltas vs docs/LAPTOP_SETUP.md worth baking in:
- `matplotlib` and `scipy` are hard imports (run_sim.py, run_slam_lite, chordspeak2 → gestures/voiced runs) but missing from the §0 pip list — add them.
- MCP python SDK 2.x renamed FastMCP; harness/server.py needs `pip install 'mcp<2'` (plus `pytest-asyncio` for the harness suite). No code changed — pinned instead.
- torch 2.13.0+cu130 from plain PyPI works on this box (RTX 3080 Ti, driver 580.x closed module): `torch.cuda.is_available()` True. The cu124 index-url in the doc is not needed anymore.
- Verified today: run_sim walks (265mm, no fall), driver 58/58, harness 21 fast + 1 slow (guard supremacy) pass, gait 6/6, gestures2 7/7 clean, eval_ppo on robust_fwd2 (ret 284.6), ppo_smoke PASS. `.mcp.json` written (venv python, ROCKY_BACKEND=sim, ROCKY_WORLD=flat).
- `cad/out` is a symlink → `../../rocky_v0.8.0_assets_cad/rocky/cad/out` (code drop shipped without it; print tooling finds STLs now). build123d NOT installed — mass_audit/CAD regen still needs the CAD extras.
- local_brain.py is hardcoded to the ollama client; this box runs llama-swap (OpenAI-compatible :8080) instead — needs the small client patch (or ollama installed) before the offline-brain demo. intent.py + --mock-llm paths verified fine without it.

2026-09-08 — laptop session 2 (Claude Code). Tyler verified the live viewer works. This pass: make chat-driving watchable/audible, wire the local fleet as the brain, get the CAD loop running here, run the queued D045 retrain. **LOCAL CODE CHANGED** — unified diff vs the v0.8.0 drop (harness/ only) at `~/Development/rocky/patches/laptop-2026-09-08-harness.patch`; attach at session 9 start.
- `harness/sim_backend.py`: (a) `ROCKY_VIEWER=1` opens the passive MuJoCo window on SimBackend and paces goto/gestures to real time (fail-soft: no display → one stderr line, headless as before — the MCP stdio channel is untouched). (b) `gesture()` now RUNS the gesture in physics (same `fn(gait, t)` tables as the playground; all 10 render clean in the flat world, no falls; `stop()` cuts one short → `stopped:"user"`; a fall reports FELL like goto). Was log-only ("session-7 demo"). (c) `ROCKY_AUDIO=1` → `say()` plays `audio/samples_v2/<word>.wav` (aplay/ffplay). (d) **goto bug fix**: the target direction was handed to the gait in the MAP frame, but WaveGait takes BODY-frame velocities. Invisible while gestures were log-only (yaw stayed ≈0); reachable the moment `turn_in_place` really turns — goto then walked off-heading and timed out. Now rotated by the current yaw; verified turn+sidestep → goto (0.25, 0) and (−0.10, 0.20) both ARRIVE within the 25 mm ball. Harness tests: 21 fast + 1 slow still green.
- `harness/local_brain.py`: `--base-url` / `ROCKY_LLM_BASE_URL` adds an OpenAI-compatible client (llama-swap, llama-server, vLLM, LM Studio) beside the ollama path; `ROCKY_LLM_API_KEY`, `ROCKY_LLM_MODEL`, `--think` (thinking is OFF by default for one-hop tool calls). **VERIFIED on the laptop fleet: qwen3.6-35b-a3b through llama-swap drove the mock robot first try** — say(greeting) → gesture(jazz_hands) → status → one-sentence report. The "first Ollama run" open item is answered without Ollama.
- `audio/samples_v2/` rendered locally (`chordspeak2.py`: 16 words + demo reel). The code drop ships without it, so the playground's `say` was silent by omission — worth a line in LAPTOP_SETUP §0.
- Local `.mcp.json`: `ROCKY_BACKEND=sim ROCKY_WORLD=flat ROCKY_VIEWER=1 ROCKY_AUDIO=1 MUJOCO_GL=glfw` → `claude` from the repo root opens the window and drives it with sound.
- CAD extras installed here (build123d 0.11.1, trimesh, shapely, rtree): `cad/run_all_checks.py` = **21/21 TREE CLEAN, 103 s** on this machine → the caliper → params.yaml → regen → checks loop no longer needs a cloud session.
- Launcher (outside the repo, fleet-specific glue): `~/Development/rocky/rocky.sh` — `play` / `chat` / `brain` / `talk` / `voice` / `test` / `jobs` / `train-recover` / `eval-recover` / `cad-check`. `voice` = push-to-talk through this laptop's whisper-server (large-v3-turbo, :8082, `/v1/audio/transcriptions`) into `harness.intent --stdin`; pipe verified with a wav, the mic run is Tyler's.
- **Caliper worksheet** (phone page; saves to a record Claude Code reads back): https://claude.ai/code/artifact/1d1a02c9-bc58-42ad-9875-902c2b371dd6 — fit-ladder rows A/D/E, kitchen-scale grams vs print_estimate.json per printed part, leg-chain 45/95/135 + deck I1 bores, dry-fit tri-states; exports a NOTES_INBOX line block.
- `train_ppo.py` nit: `--resume` with `--total-steps` ≤ steps already trained (or a different `--rollout`, so n_updates ≤ update0) dies with `UnboundLocalError: update` at the final `save(update)` instead of a message. Also: recover1 was trained at batch 1024 (8 envs × 128), not the doc's default 8 × 256.
- `eval_recover.py runs/recover1` HERE = **10/20 stood** (cloud: 12/20, "same seeds") — mujoco 3.12 / CPU float drift; the laptop baseline for any comparison is 10/20 (hold-pose 2/20, random 1/20).
- D045 capped v2 retrain (the queued laptop item) ran today, two arms at nice 10: `recover3_capped` = warm start from recover1 with `--reward v2 --log-std-max -0.5 --rollout 128 --total-steps 4000000` (+2 M steps, ~1.8 k sps) and `recover3_scratch` = same recipe from scratch, 3 M steps (~2.3 k sps). Results in the next entry.

2026-09-08 (later) — **D045 capped v2 retrain: NEGATIVE, both arms.** Same-machine `eval_recover --episodes 20` (deterministic mean action, hybrid handoff), recover1 baseline here = 10/20 stood:
- `recover3_capped` (recover1 → +2 M steps, `--reward v2 --log-std-max -0.5 --rollout 128`, 4.0 M total, 19 min): **7/20 stood**, pure-RL success 0/20, mean return 316.8, end-tilt median 5.2°; back 2/6 · side 2/7 · tumble 3/7. Final entropy **13.78 nats = exactly the cap ceiling** (15 dims × (½ln 2πe − 0.5)) — every dimension pinned at log σ = −0.5, i.e. the policy still wants maximal noise and the cap merely held it there. Training ep_len fell to 252 (stochastic rollouts DO end in success), return 543.
- `recover3_scratch` (same recipe from scratch, 3 M steps, 22 min): **3/20 stood**, pure-RL success 0/20, back 0/6 (end tilt median 113° — never rights from the back). Entropy 10.75, under the cap on its own.
- For reference: recover2 (uncapped) ended at 22.13 nats / 2/20; recover1 at 17.78 nats / ep_len 300 (never a training-time success) / 10/20.
- Reading (hypothesis, not proven): under v2 the STOCHASTIC policy gets up (episodes terminate) but the MEAN policy doesn't — the success is carried by the noise, not the mean, so capping σ removes the bang-bang symptom without moving the mean toward a righting strategy. recover1, which never saw a success term, is still the best deterministic righter. Two cheap follow-ups before another retrain: (1) evaluate recover3_capped with SAMPLED actions (several seeds) — if that scores ≫ 7/20 the mean/mode gap is the whole story; (2) RL_TOUR rung 6 (linear σ-anneal to the floor over the last third) on top of the cap, so the policy is forced to make its mean do the work. **recover1 stays the shipped righter.** Checkpoints + train_log.jsonl + train.out kept in `sim/runs/recover3_capped` and `sim/runs/recover3_scratch` (laptop only, not in any drop).

2026-09-17 — laptop session 3 (Claude Code). Tyler dry-fit the printed leg chain: base → yaw blank → fork went together "not great", the femur link attaches to nothing, the blanks don't attach well, nothing past the first servo mount connects. **Root cause is in the CAD, not the printer** — the tree's checks prove non-overlap and single-solid per part; none asks whether a joint can be assembled or what holds it. New standalone probe `cad/check_assembly.py` (NOT in run_all_checks — it fails the v0.8.0 tree on purpose; diff at `~/Development/rocky/patches/laptop-2026-09-17-cad-check_assembly.patch`). Verified numbers, all at nominal params (no caliper input needed for these):
- **Coxa stage is an assembly deadlock.** All four yaw-horn M2 driver columns pass through the crown arm (45.4 mm³ of base material in each; the in-module "driver access CLEAR" only tested against the fork). The other order — bolt fork to servo first, slide the pair in from +X — fails too: the Ø2.9 stub hits the arm for x 3..9 (up to 17.8 mm³). Lift-in-then-slide-servo leaves the screws under the arm again. No order builds it.
- Vertical budget at the coxa: horn top 38.0 = hub bottom (0 gap), floor top 45.65 vs arm underside 46.15 (0.5), collar vs arm top 0.85. Fork +1 mm → 125 mm³ into the arm. That is the "not great" even when it goes on.
- Without the 683ZZ the stub sits in a Ø6.85 pocket: 3.95 mm radial slop at z 48 — the fork wobbles on the horn alone.
- **Femur link**: each hub is 4× M2 through Ø2.4 clearance into a Ø1.7 pilot in the blank's 3 mm horn disc, then 3 mm of AIR (BCD r7 is outside the Ø6 boss), then un-piloted case top. Link pilot bore Ø6.4 vs blank centre hole Ø2.1 → no radial location. A 2 mm nudge in any direction meets zero material: nothing but screws holds it, at both ends. No M2 hardware on any order sheet.
- Blanks: a 2 mm nudge out of every cradle (yaw +X/+Z, femur −Y/+Z, knee −Y/+Z) meets zero material — retention is zip ties only (v0.1 note, never revisited). Print plan says horn disc UP, no support: a Ø20 disc on a Ø6 boss = 7 mm of 90° overhang; check_printability only flags islands and thin walls, not overhangs.
- The horn coupler (§3.4 design rule) is an orphan: only coupler_recess_demo carries the female recess; fork hub and femur link bolt straight to the horn (1709 mm³ overlap if you try to put the coupler between).
Proposed: D046 — rebuild the leg chain around its five joints (yaw horn↔fork, hip horn↔femur, femur↔knee horn, knee↔carrier, carrier↔tube), each with a fastener stack, alignment assertion across both parts, thread-engagement, driver-access-vs-everything and capture (nudge) tests in check_assembly.py, and a one-hour printed coupon per joint before any full part reprints. Not a restart: deck, shell, interfaces I1–I6 stay frozen.

2026-09-17 (later) — **D046 IMPLEMENTED: leg chain rebuilt around its joints. CAD CI 23/23 TREE CLEAN (106 s), printability 58 parts clean.** Tyler said go; photos of the old dry-fit to follow tonight. **LOCAL CODE CHANGED** — unified diff vs the v0.8.0 drop at `~/Development/rocky/patches/laptop-2026-09-17-cad-d046.patch` (cad/ only; the earlier check_assembly-only patch is folded in). STLs regenerated into the assets tree via the cad/out symlink.
- **J1 yaw (part_coxa v0.3)**: crown arm split off the base as `coxa_crown_cap` (bolts to a wider post, 2x M3 thread-forming) so the fork can be bolted to the horn from above with nothing over it; printed Ø2.9 stub replaced by an M3 x 10 AXLE screw threading 5.65 mm into a boss on the fork, shank riding the 683ZZ in the cap (inner race clamped head↔boss); horn centre-screw head pocket Ø6.6 x 3.5 (was still covered by the floor); cap ends at x 5.4 so it can drop past the collar; cap/boss gap 0.4 nominal; collar 1 mm over the cap top. Assembly order asserted in-module: servo ↓, fork ↓ (4x M2 x 6 driver columns clear of fork AND base), cap ↓, axle, femur servo ↓, strap.
- **J2/J3 hip+knee (part_femur v0.2 + part_coupler)**: femur link hubs carry the coupler's female recess on the +Y face + 2x M3 clamp clearance; the link never touches a horn. Coupler fixes: lobes moved to 45/135/225/315 (they sat over their own M2 heads — 0.8 mm under the lobe, un-droppable), half-angle 28→22 so a Ø4.4 driver fits, blind M3 bores deepened to 4.8 mm (M3 x 8 through the 3 mm web bottoms 0.2 short). Recess clamp angles are MIRRORED (−45/−225) because the coupler is flipped onto the face — that was a real bug in the first cut, caught by the new coaxiality probe. Link plate now at y −25..−19 (was −22.3..−16.3); no sim/URDF consumer of that offset.
- **Retention (new `servo_mount.py`)**: every cradle gets full-height lips (4 mm reach) over the horn-side corners + a printed strap (2x M3 into the wall tops) — `coxa_fork_strap`, `tibia_knee_strap`. Zip slots gone. 2 mm nudges now meet 300–1300 mm³ in every direction; the yaw servo is captured through the fork+axle.
- **Blank v0.2**: boss undercut filled to Ø14 (no more 7 mm overhang; Ø14 not Ø20 so the lips clear it), 4x Ø2.4 through + side-loaded M2 nut slots (4.3 x 1.9 at z 33..34.9). Screw: M2 x 8 + nut on a blank; M2 x 6 into a real (tapped) horn — VERIFY the real horn's thread on arrival.
- **Coupons (`part_leg_coupons.py`)**: boolean clips of the production solids — `coupon_j1_hub` + `coupon_j1_post` + the cap (+683ZZ, M3 x 10, 2x M3 x 8); `coupon_j2_hub` + `coupon_j2_horn` + horn_coupler (+4x M2 x 8, 4 nuts, 2x M3 x 8). Print these FIRST; a fitting coupon proves the production joint.
- **Joint suite `check_assembly.py`** is now in run_all_checks (driver columns vs everything present, drop-on paths, axle path, castellation capture, M2/M3 coaxiality, nut paths, 2 mm nudges). It failed the v0.8.0 tree with 4 findings; it is CLEAN now.
- **Hardware NOT on any order sheet (add to the next order)**: M2 x 8 pan (≥16, +spares) + M2 nuts (16) for blanks; M2 x 6 (16) for real horns; M3 x 8 (≥12: cap 2, straps 4, clamps 4 + spares); M3 x 10 (5 axles); 683ZZ (5, already listed). Thread-forming M3 into PETG everywhere — run a bolt through once after printing.
- Print-plan deltas: femur_link prints recess face UP; cap flipped (pocket up); straps flipped (bar down); fork still "hub down, support only under the collar strip" (lips are full height, no new supports). gen_print_pack.py PARTS updated; PRINT_NIGHT_s8.html not regenerated.
- **Deferred / VERIFY on hardware**: (1) does the ST3215 have a rear idler boss? If yes, hip and knee should become two-sided brackets (bending currently goes through the servo output shaft alone). (2) Case screw holes → replace lips+strap with case screws. (3) check_printability still does not flag 90° overhangs, only islands and thin walls — add an overhang rule (B-item). (4) The yaw joint deliberately has NO coupler (clamp heads would sit under the cap; smallest impact loads) — documented deviation from §3.4.

2026-09-18 (00:xx, same session) — D046 follow-through, CI still 23/23 TREE CLEAN. Patch regenerated (now also covers sim/mass_audit.py, sim/mass_budget.json, docs/decisions.md, docs/DESIGN_BACKLOG.md; viewer html and out/ excluded).
- `check_printability.py`: **cantilever rule** — an overhang blob whose boundary touches the layer below on < 60 % of its length is a cantilever (a bridge over a bore touches all round); ≥ 3 mm with a "no support" plan = hard failure. Regression: v0.1 blank horn disc → 6.7 mm FAIL, v0.2 blank 2.7 pass, tibia_sea_outer's 9.6 mm roof over the tube socket classified as a bridge (2.8 cantilever) → pass. It caught one more real one: **calib_gauge_hip's cradle shelf overhung its shaft by 5 mm at 90°, no support — a drooping reference surface.** Fixed with a lofted pedestal (34–40° from vertical). B21 in the backlog.
- Viewer rebuilt (`pebble_viewer.html`, 30.8 MB): parts row gains crown cap, femur v0.2, coupler, blank v0.2; "Print weekend" scene is now the D046 dry-fit (couplers + cap + straps posed). `test_viewer.py` not run — playwright is not in the venv.
- `PRINT_PREP_PACK.pdf` regenerated with the D046 rows (reportlab installed into the local venv — add to LAPTOP_SETUP §0).
- `sim/mass_audit.py` + `mass_budget.json`: cap ×5 (PETG) into the torso, straps into coxa/tibia. Torso 1462.4 → 1477.3 g, coxa 86.2 → 89.2, femur 15.7 → 15.1, tibia 135.0 → 138.5; robot 2649 → 2691 g (+1.6 %). Below the ±30 % fill-factor honesty band — D017/D044 numbers in bodyweights are unaffected; no re-baseline needed.
- docs: D046 row in decisions.md; B19 (two-sided hip/knee brackets if the ST3215 has a rear idler — VERIFY on arrival), B20 (case-screw retention after measuring the case), B21 (cantilever rule, shipped).

2026-09-22 — laptop session 4 (Claude Code). Tyler's photo of the 09-17 prints (coxa base with supports, fork, femur link, knee carrier, hand set, couplers, blanks, coupons) + "nothing fits, full reprint, one leg moving, then the body". Full project review (three parallel review passes: mechanical, electronics/BOM, software/repo) → `docs/REVIEW_2026-09-22.md`. **LOCAL CODE CHANGED (cad/, sim/, gait/, ros2/, docs/, bom/), all committed to git — the drop/patch workflow is over; git is the record now.**
- **Root cause of "nothing fits", one level deeper than D046: the servo model was wrong.** `cad/ref/STS3215_03a.step` (SO-ARM100) measured by ray-casting: case 45.4 × 24.8 × 28.8 flat-to-flat (v0.1 said 32), 1.5 mm rims on both faces over the rear 29 mm with the four screw holes per face IN them (top at rel. x −29.0/−7.8, bottom −32.85/−7.8, y ±10.1), a 2.6 mm plateau on the horn face (rel. x −35.2..−11.3, ±7.0), axis 10.2 from the front end (v0.1: 11.25), Ø9 output flange, horn Ø20 × 2.5 with its TOP at 33.1 (v0.1: 38.0), horn holes on a 45° pattern (v0.1: 0/90/180/270), a Ø19.9 × 2.1 rear idler on a Ø6 boss (z −1.2..−3.3), connector housing (rel. −29.85..−16.6, ±9.15, 3.3 proud) and two 3-pin headers (rel. −15..−13.1, ±1.5..8.45, 5 proud) only 3 mm behind the idler edge. All of it is in `params.servo_st3215` v2, marked "from STEP" = VERIFY.
- **D047 implemented, CAD CI 23/23 TREE CLEAN.** See decisions.md D047 for the design; `leg_frame.py` holds the three servo poses; `servo_mount.servo_cup` is one shape for all three servos. New check classes in `check_assembly.py`: cable-plug keep-outs (`servo_st3215.plug_envelope`), both ends of every slotted horn hole, rim-screw driver access against the parts present at that step, femur sweep over hip −70..90, tibia cup sweep over knee −150..−20 (in part_tibia). Printability caught three real things on the first pass (plate A hub rim 0.3 mm outside the coupler pocket → Ø30; blank rims-down had a 10.8 mm overhang → flat bottom fill; idler puck nub → omitted).
- Yaw-stage stack (leg z): plate −4..0 | fork hub 1.3..7.3 = horn top | case 11.6..40.4 | idler face 43.7 | upper plate 42.2..48.2 | hip servo 48.6.. | **hip axis 61.0** (`params.leg.hip_axis_z`; was 58 hard-coded in gait/pebble_gait.py, sim/build_mjcf.py and generate_urdf.py — all three read params now). Sim re-run: walk +X 264 mm, tilt max 1.0°, no fall; 85 fast tests pass.
- Mass audit: robot 2665 g (D046: 2691) — coxa 78 (fork 14.1), femur 34 (link 16.1 + plate B 13.5), tibia 131, torso 1449. Below the ±30 % honesty band; no re-baseline.
- URDF: `generate_urdf.py` now takes masses from `sim/mass_budget.json` and expands its one macro itself when xacro is absent; parity: FK 0.04 mm, mass 2.705 = 2.705 → PARITY OK (it was failing 3 mm / 3.09 vs 2.7 kg since D046).
- **Hardware truth still pending (the servo):** horn hole radius (STEP 14–15.6 BCD → slots), horn thread (M3 × 6 per RobotShop's kit list vs M2 lore → counterbore takes either), rim self-tapper size (Ø1.5 pilots → PA2.0?), plug stand-off (12 mm assumed). The four batch-1 coupons answer them in an evening with ONE servo in hand.
- Electronics decisions folded into `bom/SHOPPING_LIST_2026-09-22.md`: all-ST3215 (STS3250 not audit-justified, ~$88 now), Waveshare Servo Driver with ESP32 ($15.99) is core, per-leg 12 V splitters instead of daisy-chained power through 2 A pins, LiPo alarm + loop key, BNO085 on UART-RVC/SPI, foot switches to five GPIOs. Order sheet v2 typos: $252.89 not $253.89; 6 × SCS0009 = $54.
- Repo: `cad/out/pebble_viewer.html` and `PRINT_PREP_PACK.pdf` regenerated; LICENSE (MIT) added; `.mcp.json` untracked; `(1).md` duplicates removed; `docs/NEXT_SESSION.md` retired (git + BUILD_LOG carry the state).
- Photo filed: `media/2026-09-22_first_prints.jpg` (the pre-D047 leg parts — kept as the "before").

